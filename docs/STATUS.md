# 현재 상태

> **마지막 갱신:** 2026-08-04 (② 조합 실측) ·
> 🔴 **상류 배포 후보인 조합 `D1A1`을 720p GPU에서 쟀고, 게이트 안이다** — 체인 **+14.526** /
> 융합 **+12.797ms**(`gpu_sum` 차분 p50). 상류 CPU는 `D1A1+bf`를 101.2ms로 쟀다.
> **단 `bf`는 미측정이라 "상류 결론을 뒤집었다"고는 쓸 수 없다.**
> 🔴 **착수 가설의 메커니즘이 틀렸다** — *"두 arm이 LAB 변환을 공유하니 싸진다"* 는 근거가
> 코드와 어긋나 철회됐다(D1은 LAB을 쓰지 않는다). 절감은 **중간 8비트 굽기 제거**에서 나온다.
> → [REPORT_20260804_TEAM.md](REPORT_20260804_TEAM.md) · [FRAME_BUDGET.md](../FRAME_BUDGET.md) §3 주6
> 짧게 유지한다. "무엇이 끝났는가"의 진짜 기록은 `git log`다. 여기엔 **지금 어디고 다음이 뭔지**만.

## 어디까지 왔나

- **문서 체계** — 루트 규약 4개 / `docs/research/` / `docs/archive/`.
  판정선 값은 `FRAME_BUDGET.md` §1과 `lib/targets.py` **두 곳에만**. 단계별 배정치는 폐기.
- **측정 하네스** — 스키마 **v4**. D 하위 패스 열 + **두 번째 스테이지 슬롯**(`*2_ms` 3개) ·
  세션 오케스트레이터(`run_session.py`) · 런별 회수 · arm 조건 대조 · BuildConfig git 스탬프 ·
  **앱 내부 폴백 검사**(`frames_fell_back_to_passthrough != 0` → 어긋남).
- **런타임** — CameraX `Preview` → OES `SurfaceTexture` → GL 다패스 → `GLSurfaceView`.
  **arm 8개**: `passthrough` / `blit_2pass` / `gamma_only` / `drago` / `clahe_gamma` / `agcwd` /
  **`drago_clahe_chain`(8패스)** / **`drago_clahe_fused`(7패스)**.
  ⚠️ 체인이 `GpuTimerRing.MAX_PASS_COUNT = 8`을 **정확히 소진**한다 — `bf`나 ④ 오버레이를 얹으면
  계측이 꺼진다(명시적 거부라 조용히 틀리진 않는다).
- **색공간 변환 자동 계수** — 앱이 셰이더 소스에서 토큰을 세어 `session.json`에 싣는다
  (`color_transform_sites`). 주장이 아니라 텍스트와 어긋날 수 없는 값이다.
- **GPU 계측** — `GL_TIME_ELAPSED_EXT`를 코어 `glBeginQuery`로. A34에서 동작.
  ⚠️ `glQueryCounterEXT`는 `GLES30` 바인딩에 없어 **타임스탬프 방식은 불가**(JNI 필요).
- **팀 보고 완료 (2026-08-03)** — [REPORT_20260803_TEAM.md](REPORT_20260803_TEAM.md), 저장소 최초 공유.
  **동결 스냅샷이다** — 숫자가 갱신되면 `FRAME_BUDGET.md`·`docs/baselines/` 쪽이 맞다.
- **문서 숫자 독립 검증 (2026-08-03)** — 승격본 JSON 14건과 기계 대조해 **16건 정정**.
  개별 ms 전사는 거의 정확했고, 틀린 것은 **"그 숫자가 어느 런에서 나왔나"** 쪽에 몰려 있었다.

## 🔴 이번 라운드의 결론 — 조합은 게이트 안이고, 절감의 출처는 우리가 생각한 곳이 아니다

`gpu_sum_ms`의 arm 간 차분(arm − **같은 세션** `blit_2pass`). `stage_d_total_ms`가 **아니다.**

| arm | p50 | p95 | p99 | 세션 / 커밋 / 분석 창 |
|---|---|---|---|---|
| **체인 `D1A1`** (상류 cv2 구조) | **+14.526** | 14.915 | 16.200 | 08-03 `8103c2b` 642.4s |
| **융합** (중간 굽기 제거, 우리 변형) | **+12.797** | 12.671 | 12.984 | 08-04 `cdeaffc` 639.9s |
| 단품 D1 `drago` (같은 세션 재측) | +6.297 | 6.425 | 6.970 | 08-03 `8103c2b` 639.6s |
| 단품 A1 `clahe_gamma` (같은 세션 재측) | +7.152 | 7.321 | 8.313 | 08-03 `8103c2b` 642.1s |

- **융합이 체인보다 1.729ms 싸다.** 오차 한계 0.041ms(세션 간 분모 이동 0.009 + 체인 바닥 0.032)의 **42배**.
- **단품 합 13.449 대비: 체인 +1.077 비싸고, 융합만 −0.652 싸다.** 이어붙이는 것 자체는 손해다.
- 상류 `data.md`의 "② ≤20ms" 게이트를 **p99에서도** 통과(81.0% / 64.9%).
- ⚠️ **열을 바꾸면 부호가 뒤집힌다** — `stage_d_total`로 보면 융합도 단품 합보다 비싸 보인다
  (+0.221). 증분의 상당 부분이 D 열 밖(`gpu_present`)에 앉는다. → `FRAME_BUDGET.md` §3 주6
- ⚠️ 단품 3종의 결론(**폭 1.08ms, 속도로 못 갈린다**)은 그대로다 → `FRAME_BUDGET.md` §3 주3

## 승격 실측 (24건)

| 측정 | 값 | 뜻 |
|---|---|---|
| 프레임타임 (실내) | **p50 32.7 / p95 38.07~38.49ms** | 카메라 공급에 묶인 **바닥값**. arm을 얹어도 안 움직인다 |
| 프레임타임 (야간) | **p95 38.81~39.05ms** | 실내보다 tail이 조금 넓다 |
| 🔴 지연 (실내) | **p50 75.62~78.82 / p95 79.90~82.79ms** | 취득~드로우콜 **제출**. **빈 파이프라인에서 이미 예산 66.7ms 초과** |
| 🔴 지연 (야간) | **p50 91.3~93.5ms** | 실내 대비 **+13ms**. 공급 주기는 같은데 ISP가 더 붙든다 |
| B칸 | **0.63 / 0.98ms** (`drago` 런) | ⚠️ "2패스 구조에서의 B". 네 실내 런 범위 0.618~0.635 / 0.961~0.989 |
| D칸 (단품) | **6.10~7.18ms** | 폭 1.08ms. **하한** |
| D칸 (조합) | **12.80~14.53ms** | 위 표. **하한**. ⚠️ 단품과 하나의 범위로 뭉치지 말 것 |
| 런 간 노이즈 바닥 | `gpu_sum` **0.009~0.032ms** | arm마다 따로 잰다(패스 수·SSBO 구성이 다르면 변동 성질도 다르다) |
| 세션 간 분모 이동 | **−0.009ms**(08-03→08-04) / 전례 −0.062 | 항상 0.06 수준은 **아니다.** 재봐야 안다 |
| 발열 드리프트 | **없음** (11분 3등분) | 야간 11분도 같다 |

**눈으로 본 것 2건**(계측이 못 잡는다): drago가 어두운 데서 **더 잘 보였다** · 급전환 플리커 **없었다**.
🔴 단 암부 관찰은 상류 실측(대비배율 1.01x)과 어긋나고, **우리 이식이 상류보다 계통적으로 밝다는
측정**(23.5~55.6 LSB)이 있어 이탈 탓일 수 있다. 근거·한계는 `docs/baselines/README.md`.

## 다음 한 수

> **`/feature-kickoff`로 `bf`(bilateral)를 이식해 `D1A1+bf` 720p GPU 비용을 잰다 —
> 상류 배포 후보가 `D1A1+bf`인데 `bf`만 미측정이라 아직 "게이트 안"이라고 말할 수 없다.
> 체인은 `GpuTimerRing.MAX_PASS_COUNT = 8`을 이미 다 써서 `bf` 2패스를 얹으려면 그 상수를
> 먼저 올려야 한다. 분모는 `docs/baselines/20260804_indoor_blit2pass_s4bfused_a34.json`,
> 조건 A34/release/indoor_bright/11분.**

**왜 이것인가:** 이번 라운드가 조합을 게이트 안으로 넣었지만 **상류 후보의 마지막 조각이 빠져 있다.**
`bf`가 얼마인지에 따라 결론이 갈린다 — 융합 12.797에 `bf`를 더해 20ms를 넘으면 상류의
"해상도를 깎아야 한다"가 되살아나고, 안 넘으면 그 근거가 사라진다.

| 다음 | 할 일 | 왜 |
|---|---|---|
| **1** | **`bf` 이식·측정 (+ `MAX_PASS_COUNT` 상향)** | 🔴 **상류 배포 후보의 마지막 조각.** 이것 없이는 "게이트 안"이 부분적이다 |
| **2** | **I칸 — ④ 실제 사양 오버레이** | ✅ 명세 확정. 하네스 arm id(`highlight_boxes`)는 등록돼 있고 앱 구현만 남았다. **최초 범위 중 유일한 미완** |
| 3 | **지연 75.6~78.8ms 원인 조사** | 🔴 프로젝트 최대 위험. ⏸ **팀 답변 대기**(범위) |
| 4 | ③ 추론 배관 (더미 모델) | `C10`이 팀장·팀원2 공동이다. 모델 전에 ORT 경로를 뚫어둔다 |
| 5 | `gamma_only` 지속 런 재측정 | 알려진 이슈 7 |
| — | `ts`(플리커 억제) | 후보가 `D1A1+bf+ts`다. ⏸ **§B-4 계약값 대기**(리셋 조건·수렴 시간) |
| — | `A3`의 `td` | 상류가 **가설 기각**(bf에 3축 전부 패배, +10ms). 안 짓는다 |

## 🔴 팀에 막혀 있는 것 (내가 못 푸는 것)

| 대상 | 필요한 것 | 안 되면 |
|---|---|---|
| **팀장** | **§B-6 골든 이미지** — PC 레퍼런스(`scripts/lowlight.py`)는 있는데 `outputs/`가 gitignore라 출력이 없다 | 🔴 **② 품질 판정 = 이식 정확성 확인 불가.** 지금 가장 급하다 |
| 팀장 | **§B-2 동작 채널** — 상류 구현은 **LAB `L`**, 계약서 제안값은 **YUV `Y`** | 포팅 결과가 골든 이미지와 어긋난다 |
| 팀장 | **§B-4 temporal 계약값** — 리셋 조건·수렴 시간 | A3를 못 짓는다. **실측(`+ts` 성공)은 왔지만 계약값이 아니다** |
| 팀장 | **A안 정의** — 기획서 p.3(노이즈 억제 포함)과 p.6(미포함)이 다르다 | D칸 크기가 안 정해진다 |
| 팀원1 | **계약 A 확정** — 특히 해상도(아래) | 통합 학습 후엔 **재export** |
| 팀 전체 | 🔴 **표시/탐지 경로 분리 추인** (상류 요건 1) | **버짓표 직렬 전제가 유지되는지가 여기 걸려 있다** |
| 팀 전체 | **골판지 VR·스테레오 2회 렌더 전제가 살아 있는가** | I칸·표시 비용이 배가 된다 |
| 팀 전체 | **"② ≤20ms" 게이트는 기획서에 없다** — `data.md` 내부 기준이다 | 내 폰 실측으로 판정해도 되는지 불명 |
| **팀장** | 🔴 **융합을 채택할 것인가** — 체인보다 1.729ms 싸지만 **알고리즘이 바뀐다**(중간 8비트 양자화·감마 왕복 제거로 상류와 곡선이 달라진다) | 체인만 쓰면 1.73ms를 그냥 낸다. 내가 정할 수 없는 항목이다 |
| **팀장** | **상류 조합의 중간 표현 규약** — 중간 dtype·클리핑·정규화가 상류 자료에 없다 | 체인의 "중간 RGBA8"이 **추론**으로 남는다(상류 코드 인용이 아니다) |
| **팀장** | **`bf`를 잴 것인가** — 후보는 `D1A1+bf`다 | 🔴 **배포 후보의 비용이 여전히 미측정** |
| 팀 전체 | **지연 조사를 누가 파는가** | 🔴 지금 상태로 목표 FPS에 못 간다 |
| 팀원3 | **촬영 규약·초상권 방침** (계약 C = 상류 요건 3) | 대량 촬영 후엔 **재촬영 불가**. `C2`가 상류 크리티컬 패스다 |

**✅ 이번에 해소된 것 (2026-08-03 상류 대조):**

- **"② 단독 720p FPS 실측"** — 저쪽이 나에게 넘긴 항목. **A1·A2·D1 완료.** 저쪽 교착
  (*"어느 arm도 단독으로 목적을 충족 못 한다"*)에 대한 내 답: **D1이 GPU에서 6.280ms라
  CPU 82.9ms를 이유로 한 기각은 무효다.**
- **가독성·정확도 분리** — 상류 `C7` 실측으로 **결론이 났다**(분리 지지). 남은 것은 추인뿐.
- **④ 오버레이 명세** — `emphasize.py`로 확정. **I칸을 실물로 잴 수 있다.**
- **A3 `td` 가설** — 상류가 **기각**했다. 안 지어도 된다.

## 🔴 계약 A 충돌 (학습 시작 전에 잡아야 함)

| 항목 | `INTERFACES.md` §A | 기획서(공식) | 모델링 담당 `data.md` |
|---|---|---|---|
| **입력 해상도** | 640 제안 (대안 512·416) | **1×3×320×320** (p.3 §2.4) | 512×512 주류 |
| **클래스 인덱스** | `0=stairs, 1=person`, 추가는 끝에 append | 대표 위험 2~3종(계단·보행자) | COCO 80-class |
| **NMS** | ❌ 미내장 raw 출력 요청 | — | standard YOLO post-processing |
| opset | 13 제안 | 미명시 | 미명시 |

**해상도가 네 곳 다 다르다** — `FRAME_BUDGET.md` §5 레버 1의 사다리(640→512→416)에도 320이 없고
§A-3 앵커 표에도 320 행이 없다. **인덱스 재배치는 이전 측정치를 전부 무효화한다**(§A-4 불변 규칙).

## 상류 자료

| 자료 | 위치 |
|---|---|
| 아이디어 기획서 (공식 제출본, 7/29) | `docs/아이디어기획서_밤마실_20260729.pdf` (**gitignore**) |
| 모델링 담당 레포 | `kty2001/KDT_Hackathon` — **2차 대조 HEAD `c77e1d6`** (8/3) |
| **2차 대조 (최신)** | [RESEARCH_20260803_UPSTREAM.md](research/RESEARCH_20260803_UPSTREAM.md) — ③ `C4b` · `C7` 경로 분리 · ④ 명세 · 조합 arm |
| 1차 대조 (충돌 U-1~U-9) | [RESEARCH_20260731_UPSTREAM.md](research/RESEARCH_20260731_UPSTREAM.md) — 해소 현황은 2차 문서 §8 |

## 알려진 이슈

| # | 내용 | 성격 |
|---|---|---|
| 1 | **안전 회귀는 측정 불가** — 탐지 미구현. `evaluated:false` | 선행 대기 |
| 2 | **D칸은 하한이다** — 패스3의 타일 해결이 `eglSwapBuffers`에서 일어나 세 query 바깥이다. `GLSurfaceView`를 쓰는 한 못 옮긴다 | 구조적 한계 |
| 3 | **개별 D 열은 한 패스 밀려 있다** — 타일러가 FBO를 다음 패스에서 해소한다(apply가 1.24ms 벌어지는 동안 apply→present 차가 ±0.03ms 상수). **합만 신뢰할 수 있다** | 구조적 한계 |
| 4 | **계측 오버헤드를 분리할 수 없다** — 계측 on/off가 arm으로 결정돼 같은 arm A/B 불가. `render_latency` p50 1.5→3.6ms | 구조적 한계 |
| 5 | **disjoint 폐기 샘플이 0이 아니다** (최근 3런: 11 / 6 / 1개). 폐기된 것이 최악 프레임일 수 있어 **tail이 낙관 쪽으로** 치우친다 | 감시 대상 |
| 6 | `skipped_ring_full_frames`가 0이 아니면 같은 방향으로 치우친다 (현재 0) | 감시 대상 |
| 7 | **`gamma_only`의 +0.44(② 하한)는 3분 런(158.9s)이라 지속 판정선 미확인.** ✅ **D1 쪽 3분 분모 빚은 해소됐다** — 08-03 20시 세션에서 11분 분모로 재측(D1 +6.297 / A1 +7.152, 순위 불변). `gamma_only`만 남았다 | 미완(축소) |
| 16 | **🔴가 붙은 세션도 종료 코드는 0이다** — 폴백이 잡힌 런이 있어도 exit 0. 사람이 배너를 보므로 지금은 무해하지만 **CI가 exit code로 판단하게 되면 놓친다** | 미수정 |
| 17 | **`-1`이 폴백으로 읽힌다** — 레포 규약(`lib/frame_log.py`)은 `-1`을 "없는 값"으로 두는데 폴백 검사는 "일부 폴백(-1/N)"이라는 성립하지 않는 문장으로 무효 처리한다. **현재 앱은 `-1`을 낼 수 없어 도달 불가**(non-null `Int`, 0 초기화, `++`만) | 미수정(도달 불가) |
| 18 | **`docs/FRAME_LOG_SCHEMA.md`에 `render.processing` 블록 서술이 없다** — 지금 계획 대조가 근거로 쓰는 필드가 스키마 문서에 문서화돼 있지 않다 | 문서 공백 |
| 19 | **조합 arm에는 고유부/적용부 분해를 낼 수 없다** — 하위 열이 한 패스 밀려 있고(이슈 3) 융합은 패스 경계가 체인과 달라 하위 열이 대응하지 않는다. **합만 쓴다** | 구조적 한계 |
| 20 | **`outputs/`에 `fake_adb` 프로부 산출물이 남아 있다** — 검증 중 가짜 adb로 만든 것으로 `run_meta.json`의 argv로 식별된다. **승격본으로 쓰면 안 된다** | 정리 필요 |
| 8 | 조명 값 공백 정규화 불일치 — `check_lighting_condition`은 strip, `baseline_diff`는 원문 비교 | 미수정(안전 방향) |
| 9 | `analyze_frames.py`는 `meets_*_target=false`여도 exit 0. CI 판정은 `baseline_diff` 담당인데 분담이 문서에 없다 | 미수정 |
| 10 | 앱에서 **뒤로가기로 측정을 끝내면** flush가 버려질 수 있다 → 반드시 **정지 버튼** | 절차로 회피 |
| 11 | 교차검사 B는 p50만 본다 — 중간 딥슬립 1회는 놓칠 수 있다 | 한계 |
| 12 | 합성 로그가 `render_arm` 미상 경고를 낸다 (`gen_synthetic_frames.py`에 `--render_arm` 없음) | 미수정 |
| 13 | 앱이 표시 방향·화면비를 보정하지 않아 세로 화면에서 프리뷰가 누워 보인다 | 범위 밖 |
| 14 | `docs/baselines/` 승격본에 기기 **serial**이 들어 있다 (개발 기기지만 공개 시 제거 검토) | 미수정 |
| 15 | 승격 베이스라인 2건(7/30자)에는 **git 스탬프 자체가 없다** (`git_commit`/`git_dirty` 키 부재 — BuildConfig 스탬프 도입 전이다). "dirty"가 아니라 "출처 불명"이다 | 한계(과거분) |

**해소된 것:** 야간 근거 0건 · 파일명 고정으로 **PC 없이 연속 런 불가**하던 문제 ·
**로그→APK 연결고리 부재** · `pull_frames`가 낡은 로그를 조용히 가져오던 경로 ·
NaN/Inf가 폐기 가드를 통과하던 구멍 · **열별 차분을 못 쓰던 문제**(노이즈 바닥 확보로
`gpu_sum` 차분이 정당화됨) · **앱 주석의 낡은 사본**(`RenderArm.kt`가 90초 스모크 값을
모든 `session.json`에 실어 보내고 있었다).

> 🔎 **재발 주의 — "분석 창"에 `p50 × 표본수` 유도값을 적는 결함.**
> `docs/baselines/README.md`가 *"그 유도값을 적었다가 독립 검증에서 잡혔다"* 고 스스로 못 박아
> 뒀는데, **2026-08-03에 같은 방식으로 세 문서에 동시에 재발**했다(630s로 적었으나 실제
> 640.3~645.2s). 분석 창은 `t_recv_ns`의 **실제 span**이다. 문서에 옮길 때 이 줄을 먼저 볼 것.
