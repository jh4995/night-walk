# 현재 상태

> **마지막 갱신:** 2026-07-30 · **실측 0건** (실기기에서 잰 숫자가 아직 하나도 없다)
> 짧게 유지한다. "무엇이 끝났는가"의 진짜 기록은 `git log`다. 여기엔 **지금 어디고 다음이 뭔지**만.

## 어디까지 왔나

- **문서 체계 정리 완료** — 루트 규약 4개 / `docs/research/` / `docs/archive/`.
  7-28 리서치를 현재 결정사항으로 채택. **단계별 배정치는 폐기.**
  판정선 값은 `FRAME_BUDGET.md` §1과 `lib/targets.py` **두 곳에만** 있다.
- **측정 하네스 1차 완료** — 스키마 + 집계·판정 + 기기 메타 + baseline diff.
  독립 검증 2회 · 수정 3라운드(FAIL 4건, 전부 "데이터가 조용히 사라진다" 계열).
  회계 불변식으로 그 계열을 구조적으로 막았다. 상세는 `git log`.
- **Android 툴체인 준비 완료** — JDK 21(Studio 번들) · SDK android-36.1 · build-tools 36.0.0 · adb.
- **`android/` 디렉토리는 없다. 빈 파이프라인 PoC는 0%.**

## 다음 한 수

**폰 USB 재연결 후 `/feature-kickoff`로 빈 파이프라인 PoC 착수 — planner부터.**

⚠️ **측정 기기가 현재 미연결이다** (`adb devices` 빈 목록).
Galaxy A34(SM-A346N) / MediaTek Dimensity 1080 / Mali-G68 MC4 / API 36 / arm64-v8a.
에뮬레이터는 금지(가상 카메라 프레임은 실기기 숫자가 아니다)이므로 연결 없이는 측정이 무의미하다.

### PoC 착수 시 planner가 정해야 할 미결 항목

- **더미 사각형 오버레이를 PoC 범위에 넣는가?** `FRAME_BUDGET.md` §6이 I칸(④ 렌더)을
  "PoC 확장(더미 사각형 오버레이)"으로 채우게 돼 있다. 넣으면 I칸이 함께 채워진다.
- 입력 경로 추상화(카메라/영상/화면캡처)를 **첫날 설계에 넣는다** — 나중에 끼우면 카메라 경로를 다 뜯는다.

## 🔴 내가 팀을 막고 있는 것 (이번에 발견)

**모델링 담당의 ② A/B/C 판정이 내 실측을 기다리고 있다.**
저쪽 `data.md`의 게이트가 *"FPS < 20ms인 후보만 진출 / FPS 실측 후 회의 결정"* 인데,
`hardware_inference.md`가 *"실측은 전부 미착수, 숫자는 모두 문헌·경험 기반 추정"* 이라고 명시한다.
**그 실측 주체가 나다.** A안(CLAHE+감마)의 `~4.3ms@720p`도 실측이 아니다.

→ **PoC 다음은 ② A안 셰이더 실측이다.** "학습이 필요 없어 언제든 가능"이 아니라
**팀 결정을 여는 열쇠**다. ② 방식이 정해져야 3스레드 구조도 확정된다
(A면 셰이더 1패스, B/C면 매 프레임 추론이 하나 더 붙어 설계가 달라진다).

## 🔴 계약 A 충돌 (학습 시작 전에 잡아야 함)

모델링 담당 레포(`kty2001/KDT_Hackathon`) 확인 결과 — 아직 **연구·검증 단계**이고
**ONNX export 코드도 모델 아티팩트도 없다.** 그런데 이미 어긋난 것이 있다:

| 항목 | `INTERFACES.md` §A | 모델링 담당 `data.md` |
|---|---|---|
| **클래스 인덱스** | `0=stairs, 1=person`, 추가는 **끝에 append** | **COCO 80-class taxonomy** 기반 |
| **NMS** | ❌ 미내장 raw 출력 요청 | "standard YOLO post-processing assumed" |
| 입력 해상도 | 640 제안 | 미확정 (학습 데이터는 512×512 주류) |
| opset | 13 제안 | 미명시 |

**인덱스 재배치는 이전에 측정한 성능·안전 수치를 전부 무효화한다**(§A-4 불변 규칙).
저쪽 작업 노트를 읽은 것이지 계약 답변이 아니므로, `☐`를 임의로 채우지 않는다 — **직접 확인이 필요하다.**

## 알려진 이슈 (하네스)

| # | 내용 | 성격 |
|---|---|---|
| 1 | 수정 3라운드가 만든 표면(`rows_skipped`·`clock_check`·회계 불변식·임계 20배)은 **독립 검증 미실시** | 검증 공백 |
| 2 | `CLOCK_DWELL_RATIO_LIMIT = 20.0`이 실기기 데이터로 교정되지 않았다 | 실측 대기 |
| 3 | 시계 혼용 교차검사 B는 p50만 본다 — 중간에 딥슬립 1회는 놓칠 수 있다 | 한계 |
| 4 | 부분 파손 baseline(4개 지표 중 3개 null)은 여전히 `exit 0` (경고는 남) | 미수정 |
| 5 | `--out_base`가 다르고 `run_ts`가 같으면 `outputs/logs/`의 cmd·log는 덮어써진다 | 미수정 |
| 6 | **안전 회귀는 측정 불가** — 탐지 미구현. `evaluated:false`로 명시돼 있다 | 선행 대기 |

> ⚠️ **6번의 귀결:** PoC → ②A 실측으로 첫 성능 비교가 나와도, conventions §6 기준으로는
> **불완전한 보고**다. "성능 튜닝이 안전을 굶기지 않았다"는 내가 팀에서 가져온 핵심 검증 항목인데,
> 모델 아티팩트가 없어 아직 증명할 수 없다.

## 팀에 막혀 있는 것 (내가 못 푸는 것)

| 대상 | 필요한 것 | 안 되면 | 리드타임 |
|---|---|---|---|
| 팀원1 | **계약 A 확정** (위 충돌 4건) | 통합 학습 후엔 **재export** | **가장 길다** |
| 팀원3 | 촬영 규약 확정 (계약 C) | 대량 촬영 후엔 **재촬영 불가** | 길다 |
| 팀장 | ①② 픽셀 병렬화 가능 여부 (계약 B) | C칸(①) 잠김 | 중간 |
| 팀 전체 | `docs/research/RESEARCH_20260728_PIPELINE.md` §10 미결질문 5개 | 질문 1이 C·D의 **목적 자체**를 바꿈 | — |

**촬영 규약이 더 급해졌다:** 모델링 담당이 *"LoLI-Street 검증셋에 야간 프레임 0개,
포화 광원 0.00%"* 를 스스로 지적했고, **자체 촬영 야간 데이터가 필수 경로**가 됐다.

**팀에 정정해야 할 가정:** 저쪽 문서가 **QNN·NNAPI·Core ML**을 전제하고
*"최신 플래그십 NPU에서 YOLO nano 3060FPS"* 를 근거 없이 인용한다.
우리 측정 기기는 **MediaTek이라 QNN 불가**이고 A34는 플래그십이 아니다.
EP 실측 계획은 **XNNPACK → NNAPI 둘**로 줄었다.
