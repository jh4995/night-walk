# 재개 블록 — 야간 세션 전 준비

> **갱신:** 2026-08-29 · **HEAD:** `579e969` · **미커밋 10파일** (아래 §1)

## 1. 지금 상태

| 작업 | 상태 | 파일 |
|---|---|---|
| **1-7a 면적 축 도구** | ✅ **검증 통과** | `lib/stats.py` · `scripts/overlay_cost_by_boxes.py` |
| **fill 대조 arm** | ⚠ **android만 검증 통과. harness 검증 미완** | android 5 + `lib/frame_log.py` · `scripts/baseline_diff.py` · `docs/FRAME_LOG_SCHEMA.md` |

release 빌드 통과(Kotlin 경고 0). harness import OK. **커밋 안 됨.**

## 2. 재개 시 첫 할 일

🔴 **harness-verifier로 대조 arm의 harness 쪽을 검증한다** (중단됨, 산출물 없음).
대상: `lib/frame_log.py`(arm 어휘) · `scripts/baseline_diff.py`(`fill_enabled` 조건키) ·
`docs/FRAME_LOG_SCHEMA.md`. 검증 항목은 §5.

## 3. 무엇을 만들었나

### 1-7a — I칸 비용의 면적 축 (검증 통과)
- **형태 ⓐ**: 개수 버킷은 그대로, **버킷 안에서** `overlay_fill_frac` 분포를 함께 낸다.
  🔴 2차원 교차·면적 구간 버킷 **안 만든다** — 실재 런에서 표본이 안 나온다
  (야간 런 **0박스 81.6%**, 08-26 4런은 20버킷 중 8개가 n<30).
- 비율 열은 시간도 카운트도 아니라 `CONDITION_COLUMNS`로 갈랐다. `clock_of`에 비율 열이
  들어오면 죽인다(전에는 CPU 목록 밖 **모든** 열이 "GPU 타이머"로 라벨됐다).
- `summarize(values, digits=3)` 인자 추가, 비율 열만 `digits=6`.
  🔴 근거: `0.000001`이 `digits=3`에서 `0.0`, `digits=6`에서 `1e-06`으로 갈리는 것을 실행 확인.
- **v7 회귀 실측 통과**: 08-26 4런 재실행에서 개수·비용 칸 차이 **0건**
  (`boxes=0 stage_i_ms.p50=3.453 n=1269` / `boxes=1 p50=3.444 n=978`).

### fill 대조 arm — `detect_cpu_chain_highlight_nofill` (android 검증 통과)
- 🔴 **알파 0이 아니라 fill 기하를 건너뛴다.** 알파 0은 fill quad가 그대로 래스터라이즈돼
  프래그먼트 비용이 같다. 검증에서 `putQuad` 2곳이 **둘 다 `if (fill)` 안**임을 확인.
- 정점 수 분기는 `vertsPerBox(fill)` **한 함수뿐**(48 / 54). 버퍼는 최대치 유지.
- 🔴 **`drawPrepared` diff 0줄** — 블렌딩은 두 arm 모두 켠 채. 끄면 차분에 GL 상태 변경
  비용이 섞인다. 스트로크 알파 1.0이라 픽셀은 블렌딩 OFF와 비트 단위로 같다.
- `areaSum`은 `if (fill)` **밖** — 대조 arm도 `overlay_fill_frac`을 **면적 그대로** 낸다.
  🔴 `0.0`으로 내면 하네스의 `boxes_positive_fill_zero`가 거짓 비명(실증: 1193개).
- **조건키는 `fill_enabled`다.** `fill_alpha`로는 부족하다 — v7 로그의 **키 부재**와 nofill의
  **명시적 null**이 `_dig`를 통과하면 둘 다 `None`이라 "같다"로 판정된다. 실측 확인됨.
- arm enum **맨 뒤**(36→37), 앞 36개 순서 동일. 기존 arm 술어 결과 불변(가산형).

### 검증 후 고친 것 (전부 `session.json`으로 나가는 문장)
- **D1** `how_to_compare` — 🔴 nofill arm이 짝 arm 문장을 받아 **같은 JSON 안에서 모순**했다.
  전용 분기를 넣고 **야간 측정 프로토콜을 그 안에 박았다**(버킷 안에서 비교 · 면적 분포 겹침 ·
  `stage_i_ms`/`stage_d_apply2_ms`/`gpu_sum_ms` 셋을 함께 — 패스7↔8 FBO_A 병합 위험).
- **D6** `csv_columns`에 `overlay_fill_frac` 추가 — 🔴 v8 커밋이 만든 거짓 선언(CSV 4열, 세션 3열).
- **D2** `bounds_note`의 "두 오버레이 arm" → 개수를 문장에서 제거. **D5** KDoc 분리.
- 남긴 경미: D3(물리 사실 3중 기술, 기존 관행 범위) · D4(정점 수를 산문으로 재주장, 리터럴 없음).

## 4. 야간 세션 전 남은 큐

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **대조 arm harness 검증** | ⬜ 🔴 재개 첫 할 일 |
| 2 | **release 설치 + 실행 확인** | ⬜ 🔴 **아직 아무도 앱을 안 돌렸다** |
| 3 | `run_session.py` 면적 조건 배관 | ⬜ |
| 4 | `_1q` 대조 짝 만들지 여부 | ⬜ **사용자 판단 대기** |
| 5 | `gen_synthetic_frames.py` v8 | ⬜ 보류 |

### 2번이 가장 큰 미확인
컴파일은 통과했지만 **셰이더 링크에서 `aAlpha`가 지워지면 오버레이 arm이 통째로 꺼지도록**
설계돼 있다. 실기기에서만 안다. 확인할 것:
- `aAlpha` 드라이버 생존 (오버레이가 꺼지지 않는가)
- fill 외관 · 🔴 **대조 arm에서 박스 내부가 실제로 비는가**
- `session.json` 실물: `fill_enabled`가 **두 arm 모두**, 대조 arm의 `fill_alpha=null`, 정점 48
- `frames.csv`에 v8 4열, `boxes>0`에서 `fill>0`
기기 `RFCW604KXFN` 연결돼 있음. 밤 아니어도 실내에서 된다.

### 4번의 성질 — 되돌리기 어렵다
`_1q` 대조 짝(`..._nofill_1q`)이 **없으면 이번 밤에 fill 비용의 하한이 안 나온다.**
현행 `stage_i_ms`는 **상한**뿐이다(패스별 계측이 마지막 전체화면 패스를 중복 계상 — 이슈 21).
안 만든 이유: arm 36→38, 야간 런 +2, 🔴 **`_1q` arm에는 `passes[]` 서술 결함을 잡는 기계가
없다**(사람이 정점·패스 수를 손으로 센다). **상한만으로 충분한지 야간 런 구성 전에 판단.**

## 5. 대조 arm harness 검증 항목 (재개 시 그대로 위임)

1. `RENDER_ARMS`에 새 arm, 🔴 **기존 arm 순서 불변**(기계 대조), 중복 없음.
   `check_render_arm`이 `known=true`, **오타 1글자는 거부**.
2. 🔴 **조건키 3쌍**: fill↔nofill=다름 / fill↔fill=같음(경고 0) / **v7 승격본↔nofill=다름**.
   🔴 셋째에서 **`fill_enabled`(None vs False)가 유일하게 갈라야 한다** — `fill_alpha`는 못 가른다.
   그러지 않으면 이 키를 넣은 이유가 무효 → FAIL.
3. `passthrough`끼리는 경고 없음(기존 성질 유지).
4. 🔴 **승격본 전수 양방향 대조** — 새로 늘어난 조건 경고 **0건**.
5. 합성 v8 **nofill** 픽스처(면적 > 0)로 `overlay_cost_by_boxes.py` **무수정** 실행 →
   `boxes_positive_fill_zero=0` · `condition_discarded=0` · `_cross_check` 통과 · 6자리 보존 ·
   1 초과 생존. 🔴 **반례**(면적만 0.000000)에서 카운터가 실제로 튀는지도 확인.
6. `analyze_frames`가 `fill_enabled`·`fill_alpha=null`을 summary에 **그대로 싣는지**.
7. 회귀: 08-26 4런 기준 칸 불변 / 승격본 `analyze_frames` 재실행 동일(스탬프·기커밋 v8 제외).
8. 문서: "대조 arm에서 이 열은 **그리지 않은** fill 면적" · `fill_alpha` **nullable 정정** ·
   `fill_enabled` 행 · arm 표. 🔴 **열 이름 불변**(`overlay_fill_frac`).
9. `lib/targets.py`·팀 합의 문서 4개 무변경. 새 커밋 없음. `scripts/*.py` 전량 import.

## 6. 야간 세션 본체

1-6 fill 육안(형상 파악 가능한가) · 1-7 I칸 재측정(면적 축) · **fill↔nofill 차분** ·
2-7 **④ FSM 진입 4중2 실측(미측정)** · `entry4of2` baseline 승격.

### 🔴 밤에만 되찾을 수 있는 것 — 촬영 프로토콜
- **같은 박스 개수에서 면적이 갈리는 장면**(같은 대상 원거리/근거리)을 의도적으로 확보.
  없으면 버킷 안 면적 분포가 좁아 **표가 빈손이 된다.** 두 arm 모두에서.
- 클리핑 구간(큰 박스가 화면에 걸침)과 겹침 구간을 `--label`로 갈라 둔다 —
  안 그러면 양방향 함정이 상쇄된 채 평균으로 나온다.
  (겹침은 오버드로라 **비용에는 정직**하고 **가림 판정에는 거짓**. 클리핑은 반대로 **비용 과대평가**.)
- 🔴 **역균형 순서 `fill → nofill → nofill → fill`.** 같은 세션 72분에 앞자락이 **+8ms 단조
  상승**한 실측이 있다(STATUS.md) — 연속 2런만 찍으면 차분에 발열 표류가 섞인다.
- `_1q` 짝 런을 같은 세션에서 다시 뜬다(I칸 하한). 08-26 세션이 그렇게 했다.
- 🔴 **비교 단위는 런 전체 p50이 아니다** — 야간 프레임의 81.6%가 0박스라 런 p50이 차이를 씻는다.

## 7. 실측 후 처리할 문서

- 🔴 `FRAME_BUDGET.md` §3 주5 "비채움"과 I칸 `+0.562ms`가 현 빌드와 어긋나 있다.
  **팀 합의 문서라 고치지 않고 보고만 했다.** arm이 늘어 "비채움 명세에 부합하는 arm"이
  생긴 만큼 어긋남의 성격도 달라졌다.
- 정적 arm 3개(`highlight_boxes`/`_stress`/`_1q`)의 승격 숫자가 낡았다 —
  그 arm들도 `drawsOverlayFill=true`다.
- `docs/STATUS.md` 미갱신.
- 🔴 `outputs/overlay_cost_by_boxes/20260829_003336`은 **중단된 builder의 산출물**이다 —
  스탬프는 정상이지만 인용 근거로 쓰지 말 것. 삭제 여부는 사용자 판단.
- `lib/stats.py:3`(기존 줄)에 `p95 < 80ms`가 적혀 있다 — 규약 §5 "셋째 사본 금지" 위반.
  이번 변경이 넣은 것이 아니라 손대지 않았다.
