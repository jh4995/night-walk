# 밤마실 — 런타임 파이프라인 & 기술스택 결정 기록 (v0.2)

> **작성:** 팀원2 (모바일 런타임·통합·검증 오너) · **상태:** 제안 — 팀 협의/실측 대기
> **범위:** 이 문서는 **런타임 실현 방식**(무엇을 어떤 기술로 돌릴지)을 다룬다.
> 알고리즘 설계(①②=팀장)·모델 설계(③=팀원1)는 다루지 않는다 (`KICKOFF_ROLES.md` 경계).
> **읽는 법:** 각 결정은 `후보 → 선택 → 근거` 형식이며, 확정도를 3단계로 표시한다:
> **🔒 확정**(계약·플랜에서 이미 굳음) / **🟡 잠정**(내 추천, 팀 협의 필요) / **🔴 실측대기**(숫자 나와야 결정).
>
> **소유 주제:** 스택 12개 결정의 **탈락 후보와 상세 근거.** 7/28 리서치 §4는 이 표의 요약본이므로,
> "왜 이걸 골랐나 / 무엇을 떨어뜨렸나"는 여기서만 본다.
> **여기서 다루지 않음:** ms 예산 → `FRAME_BUDGET.md` / 계약값 → `INTERFACES.md` /
> 역할 경계 → `KICKOFF_ROLES.md` / 미결질문·지연 예산 → `docs/research/RESEARCH_20260728_PIPELINE.md`
>
> 📌 **v0.2에서 바뀐 것:** `docs/research/RESEARCH_20260728_PIPELINE.md`(7/28)를 현재 결정사항으로
> 채택했다. **F(모델 포맷) 확정도 🔒 → 🟡 정정**(계약 A가 아직 `☐`라 확정이 아니었음),
> `FRAME_BUDGET.md`의 옛 잠정 배정치 인용을 칸 이름 참조로 교체. **스택 선택 자체는 변경 없음.**

---

## 0. 두 장의 그림 먼저

### 런타임 파이프라인 (폰 위, 프레임당 66.7ms 예산)

```
                    ┌─────────────────── 카메라 스레드 ───────────────────┐
  [센서]  →  ① 취득  →  ② 포맷/색공간
                    └──────────────┬───────────────────────────────────┘
                                   │  (프레임 핸들 = GPU 텍스처)
              ┌────────────────────┼─────────────────────────────┐
              ▼                    │                              ▼
  ┌──── 렌더 스레드 (매 프레임) ────┐ │          ┌──── 탐지 스레드 (N프레임마다, 비동기) ────┐
  │ ③ 글레어 억제  (GPU 셰이더)     │ │          │ ⑥ 전처리(letterbox·정규화)              │
  │ ④ 저조도 개선  (GPU 셰이더)     │ │          │ ⑦ YOLO 추론  (ORT Mobile + EP)          │
  │ ⑤ 트래킹 보간  (최근 박스 유지) │◄┼──게시────│ ⑧ 후처리(decode·NMS)                    │
  │ ⑨ 선택적 강조  (오버레이 렌더)  │ │  (최근    └─────────────────────────────────────────┘
  │ ⑩ 디스플레이 합성              │ │   결과)
  └────────────────────────────────┘ │
              │                       │
              ▼                       │
          [화면] ◄────────────────────┘
```

핵심 구조 결정 하나: **③④⑨(매 프레임 경량 경로)와 ⑥⑦⑧(N프레임 탐지)를 분리한다.**
`FRAME_BUDGET.md` §4의 결론 — 탐지를 인라인 동기 실행하면 **그 프레임만 무거워져 p95가 거기 걸린다.**
이 결론은 각 단계에 몇 ms가 배정되든 성립한다.
탐지는 별도 스레드에서 돌고, 그 사이 프레임은 ⑤ 트래킹 보간이 박스를 이어받는다.

### 오프디바이스 파이프라인 (측정·MLOps, 이미 착수)

```
  [폰] frames_*.csv/session.json  ──adb pull──▶  [PC] run_utils.init_run()
                                                      │
                              outputs/<stage>/<run_ts>/  (git commit 스탬프)
                                                      │
                              baseline diff  ◀── 이전 run_ts와 비교 → 회귀 판정
```
이 트랙은 `lib/run_utils.py`로 이미 서 있다. 스택 결정은 §12.

---

## 1. 스택 한눈에 (결정 요약표)

| # | 레이어 | 최종 선택 | 확정도 | 주요 탈락 후보 |
|---|---|---|---|---|
| A | 플랫폼 | **Android 네이티브 (Kotlin)** | 🟡 | Flutter, React Native, KMP |
| B | 빌드 | **Gradle (Kotlin DSL) + AGP** | 🔒 | Groovy DSL, Bazel |
| C | 카메라 | **CameraX** (Camera2는 탈출구) | 🟡 | Camera2, NDK Camera |
| D | 추론 런타임 | **ONNX Runtime Mobile** | 🟡 | TFLite, NCNN, MNN |
| E | 추론 가속 (EP) | **XNNPACK→NNAPI→QNN 순 실측** | 🔴 | GPU delegate 고정 |
| F | 모델 포맷 | **ONNX (static shape)** | 🟡 | TFLite, torchscript |
| G | ①② 실행 기판 | **OpenGL ES 3.1 셰이더** | 🟡 | Vulkan compute, CPU, RenderScript |
| H | 프레임 버퍼 경로 | **SurfaceTexture(OES) + HardwareBuffer** | 🟡 | ImageReader CPU copy |
| I | ④ 강조 렌더 | **GL 오버레이 (PoC는 Canvas)** | 🟡 | Canvas View 고정 |
| J | 트래킹 | **경량 IoU + TTL (Kalman 확장)** | 🟡 | 전체 KLT, ByteTrack |
| K | 동시성 | **Executor 스레드 + 코루틴** | 🟡 | 순수 Thread, RxJava |
| L | 측정 하네스 | **Python 3.13 + run_utils (stdlib)** | 🔒 | pandas, 별도 DB |

> 🔒 2건(B·L)은 이미 굳어 재론 대상이 아니다. 🟡 9건이 팀 협의/내 추천,
> 🔴 1건(EP)은 실기기 숫자 없이 못 정한다.

---

# 파이프라인 단계별 상세 + 스택 근거

## A. 플랫폼 — Android 네이티브 (Kotlin) 🟡

| 후보 | 장점 | 치명적 단점 |
|---|---|---|
| **Android 네이티브 (Kotlin)** ✅ | ONNX/YOLO 계보 직결, 모바일 GPU(GLES/Vulkan) **직접 제어**, CameraX 1급 지원 | iOS 별도 |
| Flutter | iOS/Android 동시 | 실시간 프레임·GPU 셰이더는 **결국 네이티브 채널** 필요 → 초기 복잡도 폭증 |
| React Native | 생태계 | 실시간 영상 파이프라인에 부적합, 프레임 브리지 오버헤드 |
| Kotlin Multiplatform | 로직 공유 | GPU/카메라는 여전히 플랫폼별 — 공유 이득이 이 프로젝트엔 작음 |

**근거:** 병목이 **카메라 프레임 취득 + GPU 셰이더 + 온디바이스 추론** 세 곳인데, 크로스플랫폼
프레임워크는 이 셋을 전부 네이티브 채널로 뚫어야 해서 "크로스"의 이점이 사라지고 복잡도만 남는다.
`STARTUP_PLAN.md` 액션 0의 기본 추천과 일치. **iOS는 본선 시연 경로가 아니므로**(신청서상
Jetson·글래스와 같은 "여유 시 확장") 지금 지불할 비용이 아니다.

> **왜 🟡인가:** 팀 협의로 확정할 액션 0. 근거는 내 편이지만 "iOS도 보여주자"가 나오면 재론.
> 물러설 선 없음 — 크로스플랫폼은 온디바이스 실시간 목표와 정면충돌한다.

## B. 빌드 — Gradle (Kotlin DSL) 🔒

**근거:** Android 표준. 대안(Bazel)은 이 규모에 과함. Kotlin DSL은 Groovy 대비 IDE 자동완성·타입
안정성 우위. 재론 대상 아님.

## C. 카메라 — CameraX 🟡

| 후보 | 장점 | 단점 |
|---|---|---|
| **CameraX** ✅ | 생명주기 자동 관리, `ImageAnalysis`+`Preview` 동시 바인딩, 기기 파편화 흡수, 개발 속도 | 일부 저수준 레이턴시 제어를 추상화가 가림 |
| Camera2 | 노출·타이밍 완전 제어 | 보일러플레이트 대량, 기기별 분기 직접 처리 |
| NDK Camera (AImageReader) | 최저 지연, C++ | 개발 비용 최고, 팀 데모 일정에 부적합 |

**근거:** 데모 일정 안에서 카메라 파편화를 직접 감당할 여유가 없다. CameraX가
`STRATEGY_KEEP_ONLY_LATEST` 백프레셔와 YUV/RGB 출력 포맷 선택을 이미 제공한다
(`INTERFACES.md` 계약 B와 직결). **탈출구 명시:** 글래스-투-글래스 지연(`FRAME_BUDGET.md` §2)이
실측에서 허용선을 넘으면, 지연이 중요한 취득 단계만 Camera2로 내려가는 하이브리드를 검토한다.
이때 CameraX의 Camera2 Interop으로 부분 제어가 가능해 전면 재작성은 아니다.

> **왜 🟡인가:** PoC는 CameraX로 확정이나, 지연 실측 결과에 따라 취득 단계만 Camera2로 갈 여지.

## D. 추론 런타임 — ONNX Runtime Mobile 🟡

| 후보 | ONNX 수용 | 모바일 가속 | 팀 적합성 |
|---|---|---|---|
| **ONNX Runtime Mobile** ✅ | **네이티브** (변환 무손실) | NNAPI/XNNPACK/QNN EP | AID의 YOLO→ONNX 계보 직결, 계약 A가 ONNX |
| TFLite | ONNX→TF→TFLite (onnx2tf, **손실·취약**) | GPU delegate 성숙 | 변환 단계가 팀원1↔나 계약에 마찰 |
| NCNN | onnx2ncnn (양호) | Vulkan 기반 빠름 | C++ 중심, 툴링 빈약, 학습곡선 |
| MNN | 양호 | 빠름 | NCNN과 유사, 커뮤니티 작음 |

**근거:** `INTERFACES.md` 계약 A가 **모델 아티팩트를 ONNX로** 못박았다(팀원1 제공). ORT Mobile은
ONNX를 **변환 없이** 먹으므로 팀원1↔나 사이에 손실 나는 변환 단계가 끼지 않는다. TFLite는 성능은
좋지만 ONNX→TFLite 변환이 opset·연산자에 따라 깨지기 쉬워, 모델이 갱신될 때마다 변환 리스크를
반복 지불한다. AID가 이미 YOLO→ONNX 경로를 완주한 것도 계보상 이점.

> **왜 🟡인가:** 계약 A가 ONNX라 ORT가 자연스럽지만, 실측에서 특정 기기 NCNN(Vulkan)이 압도하면
> 재론 가능. 단 그 경우 계약 A에 "NCNN param/bin도 함께 export" 추가가 필요 → 팀원1 부담.

## E. 추론 가속 (Execution Provider) — 실측 순차 🔴

**이건 지금 못 정한다.** 기기 SoC(Snapdragon/Exynos/Tensor)마다 최적 EP가 다르고, 드라이버
품질 편차가 크다. **후보를 정해진 순서로 실측**하는 것이 결정이다:

| 순위 | EP | 성격 | 실측 관전 포인트 |
|---|---|---|---|
| 1 | **XNNPACK (CPU)** | 어디서나 동작, 안정 | **베이스라인.** F 칸 실측의 출발점 |
| 2 | **NNAPI** | HW 추상화 (GPU/DSP/NPU) | ⚠️ **static shape 필수** — 계약 A에서 이미 확보. 드라이버가 서브그래프를 CPU로 떨구지 않는지 |
| 3 | **QNN (Qualcomm)** | Snapdragon NPU/HTP 직접 | Snapdragon 기기에서만. 최고속 가능성, 기기 종속 |

**근거:** `FRAME_BUDGET.md` §7 질문 1(탐지 입력 해상도)과 이 EP 선택이 F 칸을 함께 결정한다.
"측정 없이 EP를 고르는 것"은 이 프로젝트의 측정 규약(conventions §4) 위반이다. XNNPACK으로
바닥을 찍고, NNAPI가 실제로 이득인지(서브그래프 파티셔닝이 깨지지 않는지) `run_utils` baseline
diff로 판정한다. 계약 A의 **static shape 결정이 여기서 값어치를 한다** — 동적 shape이면 NNAPI가
통째로 CPU fallback이라 2번이 아예 무의미해진다.

> **왜 🔴인가:** 실기기 없이 한 글자도 못 정한다. 이게 `perf-analyst`(또는 measure-harness)가
> 첫 번째로 태울 실험이다.

## F. 모델 포맷 — ONNX static shape 🟡

**근거:** 계약 A가 **제안**하는 값: ONNX, opset 13, batch 1 고정, **완전 static 입력 shape**, NMS 미내장.
static shape은 §E의 NNAPI/GPU EP가 서브그래프를 잡는 전제라 런타임 입장에서 타협 불가다. NMS 미내장은
후처리(⑧)를 런타임이 쥐어야 실기기에서 임계값을 튜닝할 수 있기 때문(계약 A-3 근거).

> **왜 🟡인가 (v0.1에서 정정):** v0.1은 이 칸을 🔒로 적었으나, `INTERFACES.md` 계약 A의 해당 항목은
> **전부 `☐` 팀원1 확정 대기 상태**다. 내가 제안한 값을 확정으로 표기한 것이 오류였다.
> 팀원1이 회신하면 🔒로 올린다.

## G. ①② 실행 기판 — OpenGL ES 3.1 셰이더 🟡

**나의 범위:** 알고리즘(어떻게 밝히나)은 팀장, **어디서 돌리나**가 내 결정.

| 후보 | 720p 픽셀 처리 | CameraX 통합 | 성숙도 |
|---|---|---|---|
| **OpenGL ES 3.1 (프래그먼트/컴퓨트 셰이더)** ✅ | GPU 병렬, 예산 내 | SurfaceTexture(OES) 직결 | 광범위 지원(minSdk 26 OK) |
| Vulkan compute | 더 세밀한 제어, 컴퓨트 우위 | 셋업 무거움, 상호운용 코드 대량 | 기기별 성숙도 편차 |
| CPU (OpenCV/네이티브) | ❌ 720p 픽셀당 처리 = 수십 ms | — | 예산 붕괴 |
| RenderScript | — | — | **API 31에서 deprecated**, 미래 없음 |

**근거:** `FRAME_BUDGET.md` C·D 칸은 **GPU 전제**다(계약 B-1). CPU로 720p를 픽셀 단위로
밝히면 프레임 예산(66.7ms) 안에 들어올 수 없다. OpenGL ES는 CameraX가 주는 external OES 텍스처를 셰이더 입력으로
바로 받아 **GPU↔CPU 왕복 없이** ①②를 처리하고 그대로 ③탐지 입력·④렌더로 넘길 수 있다.
Vulkan은 더 빠를 여지가 있으나 상호운용 보일러플레이트가 데모 일정에 부담 → **1차 GLES, 병목
확인 후 Vulkan 이관**을 검토(AID의 "단계별 프로파일 → 병목 → 타깃 최적화" 방법론 그대로).

> **왜 🟡인가:** 계약 B-1(①②가 GPU냐 CPU냐, 텍스처냐 버퍼냐)이 아직 `☐`. 팀장이 "CPU
> 프로토타입만 있다"면 그건 **레퍼런스로 받고 셰이더 포팅은 내가 한다**(계약 B-6). 팀장 알고리즘이
> 픽셀 병렬화 불가능한 형태(전역 정렬 등)면 GLES 전제가 흔들려 재설계 필요 → 계약 B-1 질문으로 상신.

## H. 프레임 버퍼 경로 — SurfaceTexture(OES) + HardwareBuffer 🟡

**이게 파이프라인 성능의 숨은 결정이다.** 프레임이 스테이지 사이를 **어떻게** 이동하느냐.

| 후보 | 데이터 이동 | 비용 |
|---|---|---|
| **SurfaceTexture(OES) → GL → (탐지용) HardwareBuffer readback** ✅ | 카메라→GPU 제로카피, 탐지 입력만 다운스케일 회수 | 취득·①②·④가 전부 GPU에 머묾 |
| ImageReader → CPU YUV → 매 단계 업/다운로드 | 스테이지마다 GPU↔CPU 왕복 | 왕복이 프레임타임을 갉아먹음(버짓 B 칸 비용이 반복됨) |

**근거:** `FRAME_BUDGET.md` B(색공간/업로드)를 **한 번만** 내려면 프레임이 GPU에 상주해야
한다. CPU 경유 경로는 ①→③→④마다 왕복이 붙어 예산을 조용히 잠식한다. 탐지(⑦)는 CPU/NNAPI
텐서가 필요하므로 **그때만** 다운스케일된 프레임을 회수(readback)한다 — 전체가 아니라 탐지 입력
해상도(§E)만. 계약 B의 "Y 평면만 처리" 제안(B-2)도 이 경로에서 색 변환을 더 줄인다.

> **왜 🟡인가:** GLES(§G)·CameraX(§C) 확정에 종속. 기기별 HardwareBuffer/OES 상호운용 함정이
> 있어 실기기 첫 빌드에서 조정 가능성.

## I. ④ 선택적 강조 렌더 — GL 오버레이 (PoC는 Canvas) 🟡

| 후보 | 시점 | 근거 |
|---|---|---|
| **Canvas View 오버레이** | **PoC 단계** | ④ 경로 증명·더미 박스에 충분, 구현 빠름 (취소한 PoC가 이 방식이었음) |
| **GL 텍스처 위 직접 렌더** | **본 파이프라인** | ①②가 이미 GL 텍스처(§H)라 같은 서피스에 강조를 그리면 합성(⑩) 왕복이 없음 |

**근거:** 단계 이행이 있는 결정이다. PoC는 "프리뷰 위에 박스가 그려지는가"만 증명하면 되니 Canvas로
빠르게. 본 파이프라인에서 ①②가 GL 텍스처에 상주하면(§H) 강조도 같은 GL 컨텍스트에서 그려야
CPU 오버레이 합성 왕복이 안 생긴다. `FRAME_BUDGET.md` I 칸(④ 렌더)은 GL 전제.

## J. 트래킹/연속성 — 경량 IoU + TTL 🟡

**⑤ 단계.** 탐지 없는 프레임(N-1개)에서 박스를 이어받아 강조가 "깜빡이지 않게" 한다.

| 후보 | 비용 | 폰 카메라 적합성 |
|---|---|---|
| **경량 IoU 매칭 + 트랙별 TTL** ✅ | 매우 쌈 (H 칸) | 2~3프레임 브리지엔 충분 |
| Kalman per-track | 약간 더 | 부드러운 보간, 추후 확장 후보 |
| 전체 KLT 옵티컬플로우 (AID 방식) | 중간 | ⚠️ **고정 카메라 전제** — 손든 폰은 전역 모션 커서 가정 붕괴 |
| ByteTrack/SORT | 무거움 | 다객체 정확하나 예산 초과 |

**근거:** AID의 KLT 연속성 아이디어는 **개념만** 전이된다(`HANDOFF.md` 갭). KLT는 고정 CCTV
전제라 걷는 폰의 큰 전역 모션에서 광류 가정이 깨진다. 밤마실이 필요한 건 "탐지가 2~3프레임
놓쳐도 강조 박스가 사라지지 않게" 하는 정도라, **IoU 매칭 + 짧은 TTL**로 충분하고 H 칸에
부담을 거의 주지 않는다. 부드러움이 문제되면 Kalman 추가. AID의 이벤트 FSM(단일프레임 오검출에 강조 안 띄우고
확정 후 표시) 사고도 여기에 얹는다 — 안전(헛경보 억제)과 직결.

> **왜 🟡인가:** N(탐지 주기) 확정(`FRAME_BUDGET.md` §7 질문 3) 후 브리지 프레임 수가 정해지면
> TTL 값이 결정된다. 실측 종속.

## K. 동시성 — Executor 스레드 + 코루틴 🟡

**근거:** §0 그림의 3스레드(카메라/렌더/탐지) 분리가 핵심 구조다. CameraX `ImageAnalysis`는
전용 `Executor`를 받으므로 탐지는 단일 스레드 Executor로 격리(순서·경합 단순화). UI/생명주기
결합부는 코루틴. RxJava는 이 규모에 과하고 팀 친숙도 낮음. 순수 Thread는 취소·생명주기 처리를
직접 해야 해 실수 여지 큼.

## L. 측정 하네스 — Python 3.13 + run_utils (stdlib) 🔒

| 후보 | 근거 |
|---|---|
| **Python + stdlib (csv/json/statistics)** ✅ | 이미 `lib/run_utils.py`로 착수. 의존성 0, 어디서나 실행 |
| pandas | 프레임 로그 분석엔 과함. 설치 부담 대비 이득 작음 (수천 행 수준) |
| 별도 시계열 DB | 해커톤 규모에 과함. `outputs/<stage>/<run_ts>/` + git 스탬프로 충분 |

**근거:** conventions §7·§8. 측정 규약(스탬프·outputs 구조)이 이미 `run_utils`에 구현돼 있고,
프레임 로그는 수천 행 규모라 stdlib `statistics`+`csv`로 백분위·집계가 충분하다. 의존성을 안 늘리는
것이 팀원 누구나 `python scripts/...` 한 줄로 재현하는 데 유리. 시각화가 필요해지면 그때
matplotlib를 **선택적 의존**으로 추가(핵심 경로는 stdlib 유지). 재론 대상 아님.

---

## 2. 결정 간 의존 그래프 (무엇이 무엇에 묶였나)

```
계약 A(ONNX static) ☐ ──┬──▶ D(ORT) 🟡 ──▶ E(EP 실측) 🔴
                        └──▶ F(포맷) 🟡
계약 B-1(GPU/텍스처) ☐ ─────▶ G(GLES) 🟡 ──▶ H(버퍼경로) 🟡 ──▶ I(GL렌더) 🟡
액션0(플랫폼) ───────────────▶ A(Android) 🟡 ──▶ C(CameraX) 🟡 ──▶ K(동시성) 🟡
FRAME_BUDGET §7 질문(해상도·N) 🔴 ─▶ E, J(트래킹) 잠금 해제
```

**읽는 법:** 🔴/☐를 풀면 그 아래 🟡들이 잠금 해제된다. 지금 병렬로 진행 가능한 것과 막힌 것:

| 지금 진행 가능 | 막힌 것 (선행 필요) |
|---|---|
| A·B·C·K (플랫폼·빌드·카메라·동시성 골격) | E (EP) ← 실기기 + 계약 해상도 |
| L (측정 하네스, 이미 착수) | G·H·I (①② 기판) ← 계약 B-1 팀장 확정 |
| 빈 파이프라인 재구축 (취소한 PoC) | J (트래킹 TTL) ← 탐지 주기 N 실측 |

---

## 3. 미결 질문 (이 문서가 팀/실측에 넘기는 것)

1. **[액션0] 플랫폼 Android 확정?** (팀 협의) — A 잠금.
2. **[계약 B-1] ①②는 GPU 셰이더 전제로 가도 되나? 알고리즘이 픽셀 병렬화 가능한가?** (팀장) — G·H 잠금.
3. **[실측] EP는 XNNPACK 베이스라인부터. NNAPI가 실제 이득인가?** (perf-analyst 첫 실험) — E 잠금.
4. **[실측] 탐지 입력 해상도(640/512/416)와 주기 N?** (`FRAME_BUDGET.md` §7) — E·J 잠금.
5. **[지연] 글래스-투-글래스 지연이 CameraX로 허용선 내인가?** — C의 Camera2 탈출구 발동 여부.

---

## 부록 — AID에서 전이되는 것 / 새로 짜는 것 (스택 관점)

| AID 자산 | 밤마실 스택에서 |
|---|---|
| YOLO → ONNX export·추론 | **전이** → D(ORT)·F(ONNX) |
| DeepStream/GStreamer 배관 | **폐기** — 폰에서 안 돎. 런타임은 새로(§0 3스레드 구조) |
| TensorRT | **폐기(본선)** → Jetson 확장 시에만. 폰은 ORT+EP(§E) |
| KLT 옵티컬플로우 | **개념만** → J는 IoU+TTL로 대체(고정 카메라 가정 붕괴) |
| 이벤트 FSM(확정 후 표시) | **전이** → J/⑨에 얹어 헛경보 억제(안전) |
| p95 tail 관리·단계별 프로파일 | **전이** → L(측정 하네스)·`FRAME_BUDGET.md` |

**코드째 이식은 `run_utils.py` 하나뿐**이라는 `STARTUP_PLAN.md` §5 원칙과 일치한다.

---

**변경 이력**
- v0.1 (2026-07-25, 팀원2) — 최초 작성. 🔒3 / 🟡8 / 🔴1. 실측·계약 확정 대기.
- **v0.2 (2026-07-29, 팀원2)** — 7/28 리서치를 현재 결정사항으로 채택. **🔒2 / 🟡9 / 🔴1.**
  F(모델 포맷) 🔒→🟡 정정 — 계약 A의 해당 항목이 아직 팀원1 확정 대기(`☐`)인데 확정으로 표기한
  오류였다. `FRAME_BUDGET.md` 옛 배정치 인용을 칸 이름 참조로 교체. **스택 선택 12건은 그대로.**
