# 밤마실 — 프레임 버짓 계산표 (v0.3)

> **작성:** 팀원2 (모바일 런타임·통합·검증) · **상태:** **A칸 실측 1건(2026-07-30). B~J는 여전히 빈칸.**
> **목적:** "720p 15FPS"라는 목표를 단계별 ms 예산으로 쪼개, 팀 전원이 같은 단위로 말하게 한다.
> **이 표의 빈칸이 곧 "누가 무엇을 재야 하는지"의 목록이다.** 각 단계 오너가 자기 칸을
> 실측으로 채우는 방식으로 살아 있는 문서로 유지한다.
>
> **소유 주제:** ms 예산 · 측정 용어 정의 · 예산 초과 시 레버.
> **여기서 다루지 않음:** 스택 선택 근거 → `PIPELINE_STACK.md` / 계약값 → `INTERFACES.md` /
> 역할 경계 → `KICKOFF_ROLES.md` / 미결질문·지연 예산 → `docs/research/RESEARCH_20260728_PIPELINE.md`
>
> 📌 **v0.2에서 바뀐 것:** v0.1의 **단계별 잠정 배정치를 전부 제거**했다.
> `docs/research/RESEARCH_20260728_PIPELINE.md`(7/28)를 현재 결정사항으로 채택한 결과다. → §8
>
> 📌 **v0.3에서 바뀐 것:** 빈 파이프라인 PoC 실측이 들어왔다. **A칸이 채워졌고**(§3),
> 글래스-투-글래스 지연의 **하한이 처음 측정됐다**(§2). B·I·J는 이 PoC로 채울 수 없음을 §6에 명시했다.
> **배정치(☐)와 판정선(§1)은 바뀌지 않았다.** → §9

---

## 0. TL;DR — 이 표가 말하는 것

1. **프레임당 예산은 66.7ms**, p95 관리선은 **80ms**. 이 둘은 목표에서 **유도된** 값이다.
2. **단계별 배분치는 여전히 없다.** 단계 비용 실측이 없는 구간에 추정치를 표에 박으면 남의 담당
   구간에 내가 예산을 배정한 모양이 되고, 나중에 실측과 어긋나면 이 문서 전체의 신뢰가 깎인다. (§3)
3. **③탐지는 별도 스레드 비동기여야 한다.** 이 결론만은 **각 단계에 몇 ms가 배정되든 성립하므로**,
   실측 전에 미리 확정할 수 있는 유일한 구조 항목이다. (§4)
4. 확정 필요한 것: **탐지 입력 해상도**, **①②가 GPU 셰이더냐 CPU냐**, **탐지 주기 N**. (§7)
5. 🔴 **지연이 FPS보다 먼저 위험해 보인다.** 처리를 하나도 안 한 상태에서 취득~제출 지연이
   **p50 78.8ms**로 측정됐다 — **이미 프레임당 예산 66.7ms를 넘는다.** 여기에 ①②③④와
   디스플레이 합성이 **더** 얹힌다. (§2)

---

## 1. 목표 정의 (숫자의 출처)

| 항목 | 값 | 근거 |
|---|---|---|
| 목표 해상도 | 1280×720 (720p) | 신청서 성능 목표 |
| 목표 처리율 | **15 FPS 이상** | 신청서 성능 목표 |
| **프레임당 예산** | **1000 / 15 = 66.7 ms** | 위 두 줄에서 **유도** |
| p95 프레임타임 관리선 | **< 80 ms** 🟡 | 팀원2 **제안** (`KICKOFF_ROLES.md` §4). 평균이 맞아도 tail이 튀면 보행자는 끊김을 느낀다 |
| 지속 성능 기준 | 위 값을 **연속 10분 후에도** 만족 🟡 | 팀원2 **제안.** 발열 스로틀링 반영. 첫 30초 숫자는 데모 근거로 못 씀 |

**헤드룸 규칙 🟡** — 기능에 배정 가능한 총량은 66.7ms의 **80%(≈53ms)까지**로 잡는다.
나머지 20%는 발열 드룹·GC·OS 스케줄링 지터용이며, 기능에 배정하지 않는다.

> **이 표에 추정치는 없다.** 66.7은 목표에서 나눈 값이고, 80ms·10분·80%는 **제안하는 기준**이다.
> 셋 다 팀 합의 대상이다 → `docs/research/RESEARCH_20260728_PIPELINE.md` §10 질문 3·5.

---

## 2. 측정 정의 (용어를 먼저 고정)

혼동을 막기 위해 **세 가지 서로 다른 숫자**를 구분한다. 미팅에서 "몇 ms 나와요?"는 항상 이 중 하나를 지목해야 한다.

| 용어 | 정의 | 목표 | 누가 잼 |
|---|---|---|---|
| **프레임타임 (frame time)** | 파이프라인이 프레임 1장을 뱉는 주기 = 1/FPS | ≤ 66.7ms | 팀원2 |
| **단계 시간 (stage time)** | 아래 표 A~J 각 구간의 소요 시간 | (미배정, §3) | 각 단계 오너 |
| **글래스-투-글래스 지연** | 실제 세상의 사건 → 화면에 그것이 뜰 때까지 | (미정, §7) · **하한 실측 p50 78.8ms** ↓ | 팀원2 |

> ⚠️ **프레임타임 66ms를 맞춰도 지연은 66ms가 아니다.** 카메라 노출·ISP 큐 + 파이프라인 +
> 디스플레이 합성이 **직렬로 쌓이므로** 실제 지연은 프레임타임보다 크고, 탐지 비동기화(§4)는
> 여기에 1~2프레임을 **더** 얹는다.
> 걷는 사람에게는 FPS보다 이 지연이 더 위험할 수 있다 — 계단 경계 강조가 반 걸음 늦게 뜨는 문제.
>
> 🔴 **하한이 측정됐다 (2026-07-30, 빈 파이프라인 PoC).** 센서 취득(`SurfaceTexture.getTimestamp()`)
> → 우리 드로우콜 제출까지가 **p50 78.8 / p95 82.8 / p99 84.6ms** (A34, release, 처리 0, 분석 창 642.5s).
> 근거: `docs/baselines/20260730_poc_empty_a34.json` (run_ts `20260730_225002`).
>
> 🔴 **야간에 더 나빠진다 (2026-08-01 실측).** 같은 값이 `outdoor_night_lit` **91.3** /
> `outdoor_night_dark` **91.5** / 야간+Drago **93.5ms** — 실내 대비 **+13ms**다.
> **공급 주기는 안 변했는데**(§3 주1 정정) 취득~제출만 늘었다 = ISP가 프레임을 더 오래 붙든다.
> 근거: `docs/baselines/20260731_night_*.json`. **이 앱의 실제 사용 조건에서 지연이 더 크다.**
>
> **이건 전체 지연이 아니라 하한이다** — 디스플레이 합성·패널 응답이 빠져 있고, ①②③④도 아직 없다.
> ⚠️ 그리고 이 값에는 **GPU 실행 시간이 안 들어 있다**(`t_render_end_ns`는 드로우콜 **제출** 시각이다).
> Drago의 GPU 6.28ms는 여기 **더해져야** 한다.
> 그런데 이 하한만으로 **프레임당 예산 66.7ms를 이미 넘는다.** 프레임타임(32.7ms)이 예산의
> 절반이어도 지연은 그렇지 않다는 뜻이며, §7 질문 6(지연 목표를 숫자로 정할 것인가)이
> **정성 확인으로 넘길 사안이 아님**을 시사한다.
>
> 이 값이 유효한 이유: 이 기기의 `t_capture_ns` 기준 시계가 우리 로그 시계와 같은 `CLOCK_BOOTTIME`으로
> **실측 판별**됐다(두 시계 오프셋 ≈6.2일이라 배타적으로 갈렸다). 기준이 달랐다면 이 값은 전량
> 폐기되고 지연은 미측정으로 남았을 것이다 — 다른 기기에서는 다시 판별해야 한다.
>
> 구조 A(글라스 카메라 + Wi-Fi)에서는 왕복 지연이 여기에 **합산**된다 →
> `docs/research/RESEARCH_20260728_PIPELINE.md` §2.

---

## 3. 프레임 버짓표 (본표)

**시나리오 기준:** 720p 입력, 탐지는 N프레임마다 별도 스레드 (N은 미확정, §7 질문 3).
**"실측" 칸이 이 문서의 진짜 산출물이다. 지금 채워진 것은 A칸 하나뿐이다.**

| # | 단계 | 오너 | 실행 빈도 | 배분 | **실측 (ms)** | 측정 방법 |
|---|---|---|---|---|---|---|
| A | 카메라 취득 (ISP→프레임 버퍼 도착) | 팀원2 | 매 프레임 | ☐ | **32.5 / 38.1** (p50/p95) ⚠️**간격이며 비용이 아니다** → ↓주1 | PoC 프레임 콜백 간격 |
| B | 색공간 변환 / GPU 텍스처 업로드 (YUV→RGB) | 팀원2 | 매 프레임 | ☐ | **0.63 / 0.98** (p50/p95) ⚠️**2패스 구조에서의 값** → ↓주3 | GPU 타임스탬프 쿼리 |
| C | **① 눈부심 억제** (글레어 제어) | 알고리즘 팀장 / 구현 팀원2 | 매 프레임 | ☐ | — ⚠️**D와 합쳐질 수 있다** → ↓주4 | 셰이더 구간 타이머 |
| D | **② 저조도 개선** (대비 복원) | 알고리즘 팀장·팀원1 / 구현 팀원2 | 매 프레임 | ☐ | **6.28 / 6.48 / 7.34** (p50/p95/p99) — Drago, **하한** → ↓주3 | 셰이더 구간 타이머 |
| E | **③ 위험 탐지** (YOLO) — 전처리 | 팀원1(모델) / 통합 팀원2 | N프레임마다 | ☐ | — | 리사이즈·정규화·텐서화 구간 |
| F | **③ 위험 탐지** — 추론 (ORT Mobile) | 팀원1 / 통합 팀원2 | N프레임마다 | ☐ | — | ORT 프로파일러 |
| G | **③ 위험 탐지** — 후처리 (NMS/디코드) | 팀원1 / 통합 팀원2 | N프레임마다 | ☐ | — | 후처리 구간 타이머 |
| H | 트래킹 / 연속성 보간 (탐지 없는 프레임의 박스 유지) | 팀원2 | 매 프레임 | ☐ | — | 구간 타이머 |
| I | **④ 선택적 강조 렌더** (경계 윤곽선) | 팀원2 | 매 프레임 | ☐ | — | 드로우콜 GPU 타이머 |
| J | 디스플레이 합성·제출 (SurfaceView/Choreographer) | 팀원2 | 매 프레임 | ☐ | — | vsync 대비 제출 시각 |

> **주1 — A칸 숫자를 다른 칸과 더하지 마라.** A칸의 측정 방법은 "프레임 콜백 간격"이므로
> 나온 값은 **카메라가 프레임을 주는 주기**이고, 예산에서 차감하는 **소요 시간이 아니다.**
> 30fps 요청에 묶인 값이라(33.3ms) 이 숫자는 카메라 설정이 바뀌면 같이 바뀐다.
> **여유의 상한이 아니라 바닥값이며, 여기서부터 ①②③④ 비용이 더해진다.**
> 조건: A34 / release / `pipeline_stages: []` / 조명 `indoor_bright` / p99 40.6 / max 49.6 /
> **warmup 이후 분석 창 642.5s**(세션 전체 672.5s) — `t_recv_ns` 실제 span이며 `p50×n` 유도값이 아니다.
> 근거: `docs/baselines/20260730_poc_empty_a34.json` (run_ts `20260730_225002`), 같은 조건 반복 런은
> `..._repeat.json`. **런 간 노이즈 p50 −0.12% / p95 +0.47% / p99 +1.10%.**
>
> ⚠️ **이 숫자는 야간 근거가 아니다.** 라벨이 `indoor_bright`다.
> 🔎 **2026-08-01 정정:** 위 줄이 걱정한 *"저조도에서 AE가 공급 주기를 벌린다"* 는
> **이 기기에서 반증됐다.** 야간 실측 `recv_interval` p50 32.60~32.77 vs 실내 32.46 —
> 어둠 속에서도 30fps를 그대로 준다. 다만 **지연은 야간에 +13ms 나빠진다**(§2).
> ⚠️ **안전 회귀는 미평가다**(탐지 미구현, `evaluated: false`). 이 숫자는 안전에 대해 아무것도 증명하지 않는다.
>
> **주3 — B·D 칸을 읽는 법 (2026-08-01 실측).** A34 / release / `git_dirty=false`(`d011c94`) /
> `indoor_bright` / **분석 창 640s**(지속 판정선 통과) / GL_EXT_disjoint_timer_query.
>
> | | `blit_2pass` | `gamma_only` | `drago` |
> |---|---|---|---|
> | ② 자리 (`stage_d_total_ms`) | 1.179 | 1.403 | 5.757 |
> | `gpu_present_ms` (**세 arm 동일 코드**) | 1.881 | 2.087 | **3.582** |
> | `gpu_sum_ms` | 3.737 | 4.174 | 10.017 |
> | **② 증분 = `gpu_sum` 차분** | (분모) | **+0.44** | **+6.28** |
>
> 🔴 **D칸 값은 `stage_d_total_ms`가 아니라 arm 간 `gpu_sum` 차분이다.** 이유: 세 arm에서
> **글자 그대로 같은 코드**인 `gpu_present_ms`가 1.881 → 3.582로 움직인다. ② 증분의 **27%가
> D 열 밖(present)에 앉는다** — `stage_d_total` 차분 4.58ms만 인용하면 실제 6.28ms를 과소로 낸다.
> 이것이 런 간 변동이 아님은 **같은 arm 반복 2런으로 확인**했다(노이즈 바닥 표 아래).
>
> ⚠️ **6.28ms도 하한이다.** 패스3의 타일 해결이 `eglSwapBuffers`에서 일어나는데 그건 세 query
> 전부의 **바깥**이다(`GLSurfaceView`를 쓰는 한 옮길 수 없다).
> ⚠️ **B칸은 "2패스 구조에서의 B"다.** §6 주2가 지적한 대로 OES 경로에서 B는 원래 샘플러가
> 흡수한다 — 오프스크린 패스를 명시적으로 둔 구조에서의 값이라 **같은 이름의 다른 물리량일 수 있다.**
> ⚠️ **D는 Drago 기준이다.** ② 방식이 확정되지 않았다 — CLAHE·AGCWD는 미측정이고,
> `gamma_only`의 +0.44ms가 ② **하한**이다.
>
> **런 간 노이즈 바닥** (같은 arm `drago` 반복 2런, 각 3분): 전 열이 **0.02ms 미만**
> (`stage_d_analyze` 0.000 / `gpu_present` 0.009 / `gpu_sum` 0.016). 위 6.28ms는 바닥의 **390배**다.
> **발열:** 11분 런을 3등분해도 드리프트 0.01ms 수준이고 프레임타임 p95는 오히려 좁아진다
> (38.555 → 38.286). 야간 11분 런도 같다.
>
> 근거: `docs/baselines/20260801_indoor_*.json` 6건 · `20260731_night_*.json` 3건.
>
> **주4 — C칸이 D칸에 흡수될 수 있다.** Drago는 로그 압축이라 **강광원을 실제로 누른다**
> (상류 실측: 상위 1% 밝기 −81, 포화 면적 0.67%→0.04%). 그건 ①의 역할이다.
> 상류 `lowlight_classical.md` 결론 4가 *"local tone mapping은 강광원 억제와 암부 증폭을 한 연산으로
> 하므로 ①을 별도 단계로 두지 않아도 될 가능성이 있다"* 고 적었다. **②가 Drago로 확정되면
> C·D를 하나로 합치는 것이 팀 판단 대상이다** — 위 6.28ms는 그 경우 C+D다.
>
> 📌 **왜 나머지 숫자를 안 채웠나.** v0.1에는 단계별 잠정 배정치가 있었으나 v0.2에서 전부 제거했다.
> 단계 비용 실측이 없는 구간에 추정치를 박으면 (a) **남의 담당 구간에 내가 예산을 배정한 모양**이 되고
> (b) 나중에 실측이 어긋났을 때 **이 문서 전체의 신뢰가 깎인다.**
> **채워야 할 칸의 목록**이 지금 시점에 낼 수 있는 정직한 산출물이다.
>
> 같은 이유로 **경로별 합계와 산술 검증도 뺐다.** 배정치가 없으면 합계도 없다.
> 대신 배정치와 무관하게 성립하는 구조 결론을 §4에 둔다.

---

## 4. 핵심 결론 — 탐지는 비동기여야 한다

**이 결론은 각 단계에 몇 ms가 배정되든 성립한다.** 그래서 탐지 실측이 없는 지금도 확정할 수 있다.

탐지(YOLO 추론)는 파이프라인에서 **단일 최대 비용 단계**다. 그래서 매 프레임이 아니라
N프레임마다 돈다 — 여기까지는 팀이 이미 같은 방향이다.

문제는 그 다음이다. **N프레임마다 돌리면 그 프레임만 무거워진다.** 가벼운 프레임 여러 장과
무거운 프레임 한 장이 번갈아 나오면:

- 평균 프레임타임은 목표를 통과할 수 있다
- 그러나 **p95는 그 무거운 프레임에 걸린다** — 걷는 사람은 주기적인 끊김으로 체감한다

**채택 설계 (팀원2 제안):**
```
[카메라 스레드]  A → B ─┬─→ [렌더 스레드] C → D → H → I → J   (매 프레임)
                        └─→ [탐지 스레드] E → F → G  (N프레임마다, 완료 시 최신 결과 게시)
```
- 렌더 경로는 **탐지 완료를 기다리지 않는다.** 항상 "가장 최근에 완료된 탐지 결과"를 쓴다.
- 그 사이 프레임은 **H(트래킹/연속성 보간)**이 박스를 이어받는다 — AID의 KLT 연속성 아이디어가
  개념만 여기로 전이된다(고정 카메라 전제라 알고리즘 자체는 IoU+TTL로 대체, `PIPELINE_STACK.md` §J).
- **대가:** 강조 오버레이가 실제 장면보다 **1~2프레임 늦다.** §2의 지연 문제와 합산된다.
  → 허용 여부는 팀 합의 필요(§7 질문 4). 이 지연이 보행 안전에 허용 범위인지는
  **야간 코스 정량 확인(팀원2 담당)**에서 검증할 항목.

> **"평균 15FPS"만 목표로 두면 이 문제가 안 보인다.** p95를 목표에 함께 넣자고 제안하는(§1)
> 근거가 이것이다.

---

## 5. 예산 초과 시 레버 (미리 정해두는 대응 순서)

실측이 예산을 넘겼을 때 **무엇부터 깎을지 미리 합의해 두면**, 데모 3일 전 즉흥 결정을 피할 수 있다.
위쪽이 먼저 당길 레버(비용 대비 효과 좋고 안전 영향 적음), 아래로 갈수록 안전에 손대는 레버.

| 순위 | 레버 | 예상 회수 | 잃는 것 | 결정권 |
|---|---|---|---|---|
| 1 | **탐지 입력 해상도 축소** (640→512→416) | F가 대략 해상도² 비례로 감소 | 작은/먼 객체 recall ↓ | 팀원1 + 팀원2 |
| 2 | **탐지 주기 N 증가** (3→4→5) | 평균 프레임타임 ↓ (p95는 그대로) | 강조 갱신 지연 ↑ | 팀원2 |
| 3 | **②저조도 개선 커널 경량화** | D 일부 | 대비 복원 품질 ↓ | 팀장(알고리즘) |
| 4 | **①②를 GPU 셰이더로 강제** (CPU 구현이면) | C+D 대폭 | 구현 난이도 ↑ | 팀원2 |
| 5 | **처리 해상도 하향** (720p→540p, 표시는 업스케일) | A~D, H~J 전반 | 화질·경계 선명도 ↓ | 팀 합의 |
| 6 | **표시 FPS 목표 하향** (15→12) | — | **신청서 목표 미달** | 팀 합의 (최후) |

> 신청서에도 "15FPS 미달 시 해상도 하향 / 탐지 주기 조정"이 적혀 있다(레버 1·2·5).
> **레버 6은 신청서 수치를 깨는 것이므로 팀원2 단독으로 당기지 않는다.**

**안전 가드레일:** 레버 1·2는 위험물 강조 품질을 직접 깎는다.
성능 튜닝 전후로 **동일 야간 클립에서 위험물 강조 누락률이 악화되지 않았음**을 함께 보고한다.
성능 회귀 검증과 안전 회귀 검증은 **한 세트로 돈다** — 이게 팀원2가 가져온 검증 항목의 실체다.

---

## 6. 아직 안 채워진 칸을 누가 언제 채우나

| 칸 | 채우는 사람 | 채우는 시점 | 선행 조건 |
|---|---|---|---|
| **A** ✅ | 팀원2 | **빈 파이프라인 PoC — 완료 (2026-07-30)** | 없었음. 프레임타임 실측 **32.7ms(p50)** 가 전체 예산의 분모로 확보됐다 |
| **B** ✅ | 팀원2 | **완료 (2026-08-01)** — §6 주2의 선택지 (a)를 실행했다 | GL timer query가 Mali-G68에서 동작함을 확인하고 3패스 구조를 세웠다. 단 "2패스 구조에서의 B"라는 단서가 붙는다(주3) |
| **D** ✅ | 팀장(알고리즘) → 팀원2 셰이더 구현 | **Drago 기준 완료 (2026-08-01)** | ②가 아직 A/B/C 미확정이라 **후보 하나의 값**이다. CLAHE·AGCWD는 미측정 |
| C | 팀장(알고리즘) → 팀원2 | ①② 모듈 시그니처 확정 후 | **D에 흡수될 수 있다**(주4). CPU 버퍼냐 GPU 텍스처냐 (`INTERFACES.md` 계약 B-1) |
| I · J ⚠️ | 팀원2 | **아직 채울 수 없다** → ↓주2 | 오버레이(④)가 있어야 잴 대상이 존재한다 |
| E · F · G | 팀원1 → 팀원2 통합 | 모델 아티팩트 수령 후 | ONNX opset·입력 해상도·NMS 내장 여부 (`INTERFACES.md` 계약 A) |
| H | 팀원2 | 탐지 연동 후 | E~G 동작 + 탐지 주기 N 확정 |

**의존 관계 한 줄:** `INTERFACES.md`(계약)가 C·D·E·F·G의 전제고, **빈 파이프라인 PoC**가 A를 채웠다.
→ 이 표를 먼저 낸 이유: **빈 칸의 모양이 곧 남에게 무엇을 요청해야 하는지의 목록**이 된다.

> **주2 — B·I·J 배정과 "처리 없음"은 동시에 성립하지 않는다 (팀 판단 필요).**
> v0.1~v0.2는 A·B·I·J를 한 묶음으로 "빈 파이프라인 PoC / 선행 조건 없음"에 배정했으나,
> 각 칸의 측정 방법(§3)과 대조하면 A만 성립한다:
>
> | 칸 | §3이 정한 측정 방법 | 왜 이 PoC로 안 되나 |
> |---|---|---|
> | **B** 색공간 변환·업로드 | GPU 타임스탬프 쿼리 | 처리가 없으면 **변환 자체가 없다.** OES 경로에서는 샘플러가 흡수해 독립 비용으로 분리되지 않는다 |
> | **I** ④ 강조 렌더 | 드로우콜 GPU 타이머 | 오버레이가 없으면 **잴 대상이 없다.** `PIPELINE_STACK.md` §I는 I칸을 GL 전제로 두었고 PoC 오버레이는 Canvas라 **다른 물리량**이다 |
> | **J** 디스플레이 합성·제출 | vsync 대비 제출 시각 | `Choreographer`/`FrameMetrics` 계측이 필요하고 프레임 로그 스키마에 담을 열이 없다 |
>
> 참고로 이번 PoC가 낸 `render_latency` **p50 1.7ms**는 I칸이 **아니다.** 드로우콜 제출까지의
> **CPU 비용**이고 GPU 실행 시간이 아니다 — "렌더가 사실상 무료"로 읽으면 틀린다.
>
> → **선택지는 둘이고 팀이 정한다:** (a) PoC 범위를 GL 계측(timer query)과 오버레이까지 넓힌다,
> (b) B·I·J를 후속 단계로 이월한다. **배정 자체는 팀원2가 임의로 바꾸지 않았다.**
> 현재 계획은 (b)이며, ②저조도 셰이더 실측 때 GL 계측을 함께 세워 두 번 짓지 않는 쪽이다.

---

## 7. 팀에 던질 미결 질문

1. **탐지 입력 해상도는?** (팀원1) — 640? 512? 416? F 실측의 전제이자 EP 선택과 함께 F 칸을 결정한다.
2. **①②는 GPU 셰이더 전제로 가도 되나?** (팀장) — CPU 구현이면 C·D가 프레임 예산 안에 들어오지 못한다.
3. **탐지 주기 N의 초기값은 3으로 시작해도 되나?** (팀 전체) — 실측 후 조정 전제.
4. **오버레이가 1~2프레임 늦는 것을 허용하나?** (팀 전체) — §4의 대가. 허용 못 하면 설계가 바뀐다.
5. **목표를 "평균 15FPS + p95 < 80ms"로 갱신하는 데 합의하나?** (팀 전체) — `KICKOFF_ROLES.md` §4 제안.
6. 지연(글래스-투-글래스) 목표를 숫자로 정할 것인가, 정성 확인만 할 것인가?

> 위 6개는 **버짓 칸을 직접 잠그는** 질문이다.
> 이와 별개로 **구조를 가르는** 미결 질문 5개가
> `docs/research/RESEARCH_20260728_PIPELINE.md` §10에 있다 — 특히 질문 1(①②의 결과를 사용자가
> 어디서 보는가)은 C·D 칸의 **목적 자체**를 바꾼다.

---

## 8. 변경 근거 — v0.1의 잠정 배정치를 왜 뺐나

v0.1(7/24)은 단계별 잠정 배정치(합계 기준 경량 경로 38ms / 중량 경로 68ms)를 싣고, 거기서
"탐지 동기 실행 시 예산 초과"라는 산수 결론을 끌어냈다.

7/28 리서치에서 이 접근을 뒤집었고, **그 판단을 현재 결정사항으로 채택한다:**

| | v0.1 (7/24) | **v0.2 (채택)** |
|---|---|---|
| 단계별 배분 | 잠정 ms 배정 | **빈칸(☐)** — 오너가 실측으로 채움 |
| 지연 | "프레임타임의 2~3배(130~200ms)" | **미측정** — 크다는 방향만 확정 |
| 탐지 비동기 근거 | 38 vs 68ms **산수** | **구조 논증** — 배분치와 무관하게 성립(§4) |

**이유:** 추정치를 표에 박으면 남의 담당 구간에 예산을 배정한 모양이 되고, 실측과 어긋나면
문서 전체의 신뢰가 깎인다. 그리고 비동기 결론은 **산수 없이도 성립**하므로 추정치가 지탱하고
있던 것이 애초에 없었다.

> ⚠️ **이 문서의 옛 배정치(6.0 / 4.0 / 22.0 / 3.0 ms 등)를 인용한 자료가 있으면 그것도 무효다.**
> `INTERFACES.md`·`PIPELINE_STACK.md`의 해당 인용은 v0.2에 맞춰 함께 정리했다.

---

## 9. 변경 근거 — v0.3에서 무엇이 실측으로 바뀌었나

**2026-07-30, 빈 파이프라인 PoC 실측.** Galaxy A34(SM-A346N, MediaTek Dimensity 1080, Mali-G68) /
release 빌드 / 실기기 / 720p 30fps 요청 / 처리 0 / 조명 `indoor_bright` / **연속 10분 이상 × 2회**
(warmup 이후 분석 창 **642.5s · 648.2s**, 세션 전체 672.5s · 678.2s).

| | v0.2 (7/29) | **v0.3 (실측)** |
|---|---|---|
| A칸 | ☐ 미측정 | **32.5 / 38.1 ms** (p50/p95) — 단, **간격이며 비용이 아니다**(§3 주1) |
| 프레임타임 | 미측정 | **32.7 / 38.1 / 40.6 ms** (p50/p95/p99), 평균 29.9 FPS |
| 지연 | **미측정** — 크다는 방향만 | **하한 p50 78.8ms 실측** — 처리 0에서 이미 예산 초과(§2) |
| B · I · J | A와 한 묶음으로 PoC 배정 | **이 PoC로 불가**임을 명시(§6 주2). 배정 자체는 안 바꿨다 |
| 지속 성능 | 기준만 제안(§1) | **642.5s·648.2s 런에서 tail 악화 없음** — p99 40.6→41.1 |

**측정 방법론이 신뢰할 만한 근거:**
- 두 런의 **행 회계가 닫혔다** (`rows_read = rows_used + warmup`), 폐기 샘플 0, 시계 교차검사 위반 0
- **런 간 노이즈 p50 −0.12% / p95 +0.47% / p99 +1.10%** — 같은 조건 반복이 재현된다
- 판정선은 코드(`lib/targets.py`)에서 왔고 앱에는 숫자가 없다. **폰은 타임스탬프만 남기고 판정은 PC가 한다**

**이 실측이 말하지 않는 것 (같이 읽어야 한다):**
- **야간 성능** — 라벨이 `indoor_bright`다. 저조도 AE가 공급 주기를 벌리므로 별도 측정 필요
- **①②③④가 붙은 프레임타임** — 32.7ms는 카메라 공급에 묶인 **바닥값**이다
- **안전 회귀** — 탐지 미구현으로 `evaluated: false`. **성능만 있는 보고는 불완전한 보고다**
- **발열 상한** — 처리가 없어 스로틀링할 부하가 없었다. ①②③④ 이후 재측정 필요

**팀에 던지는 것:** §7 질문 6(지연 목표를 숫자로 정할 것인가)은 이제 **정성 확인으로 넘기기 어렵다.**
처리 0의 하한이 예산을 넘었으므로, 목표를 정하지 않으면 나중에 "느린데 기준이 없어 판정 불가"가 된다.

---

## 부록 — 참고 상수

| 항목 | 값 |
|---|---|
| 15 FPS | 66.7 ms/frame |
| 20 FPS | 50.0 ms/frame |
| 30 FPS | 33.3 ms/frame |
| 60Hz 디스플레이 vsync 간격 | 16.7 ms |
| 720p 픽셀 수 | 921,600 px |
| 540p(960×540) 픽셀 수 | 518,400 px (720p의 **56%**) |

> **vsync 양자화 주의:** 60Hz 화면에서 프레임타임 66.7ms는 vsync 4틱(66.8ms)에 아슬아슬하게 걸린다.
> 실제 표시 주기는 4틱과 5틱(83.4ms)을 오가며 **체감 저더**를 만들 수 있다.
> 프레임타임을 50ms(3틱) 근처로 만드는 편이 15FPS를 억지로 맞추는 것보다 매끄럽게 보인다 —
> 헤드룸 규칙(§1)을 둔 두 번째 이유.

---

**변경 이력**
- v0.1 (2026-07-24, 팀원2) — 최초 작성. 전 항목 잠정 배정치, 실측 0건.
- **v0.2 (2026-07-29, 팀원2)** — `docs/research/RESEARCH_20260728_PIPELINE.md`(7/28)를 현재 결정사항으로
  채택. **단계별 잠정 배정치 전면 제거**(§3), 지연 추정치 "130~200ms" 철회(§2),
  탐지 비동기 근거를 산수→구조 논증으로 교체(§4). 변경 근거는 §8. **실측 여전히 0건.**
- **v0.3 (2026-07-30, 팀원2)** — **첫 실측.** 빈 파이프라인 PoC로 **A칸을 채웠고**(§3 + 주1),
  글래스-투-글래스 지연의 **하한 p50 78.8ms를 처음 측정**했다(§2). B·I·J는 "처리 없음"이라는
  PoC 정의와 충돌해 채울 수 없음을 §6 주2에 명시했다 — **배정 자체는 바꾸지 않았고 팀 판단으로 남겼다.**
  변경 근거·한계는 §9. **§1 판정선(66.7 / 80 / 10분 / 80%)과 B~J 배정치(☐)는 그대로다.**
- **v0.4 (2026-08-01, 팀원2)** — **B·D 칸을 실측으로 채웠다**(§3 + 주3). GL timer query가
  Mali-G68에서 동작함을 확인해 §6 주2의 선택지 **(a)를 실행**한 결과다 — 팀 판단으로 남겼던
  항목이 수단 확보로 풀렸다. **D는 `stage_d_total`이 아니라 arm 간 `gpu_sum` 차분**임을 실측으로
  못 박았고(동일 코드인 `gpu_present`가 1.881→3.582로 움직인다), 같은 arm 반복 2런으로
  **노이즈 바닥 0.02ms 미만**을 확보해 그것이 변동이 아님을 보였다.
  §2에 **야간 지연 +13ms**를 추가하고, §3 주1의 *"저조도에서 공급 주기가 벌어진다"* 는
  **야간 실측으로 반증**해 정정했다. §6 표에서 B·D를 완료로, C를 "D에 흡수될 수 있음"으로 바꿨다(주4).
  **§1 판정선과 나머지 칸의 배정치(☐)는 그대로다.** 근거는 `docs/baselines/`의 9건(실내 6·야간 3).
  ⚠️ **D는 Drago 후보 하나의 값이고 하한이다. ② A/B/C는 여전히 미확정이며 안전 회귀는 미평가다.**
