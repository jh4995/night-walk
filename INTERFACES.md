# 밤마실 — 4단계 모듈 인터페이스 규약 (v0.2)

> **작성:** 팀원2 (모바일 런타임·통합·검증) · **상태:** 제안 — 각 계약의 상대방 확정 대기
> **목적:** 4단계 파이프라인의 모듈들이 런타임에 꽂히는 **계약**을 먼저 못 박아, 4명이 서로를 기다리지 않고
> 병렬로 일하게 한다. 계약이 없으면 각자 만든 것이 마지막 주에 안 맞는다.
>
> **소유 주제:** 계약 A(모델 아티팩트) · B(①② 시그니처) · C(녹화 입력)의 값과 인수 조건.
> **여기서 다루지 않음:** ms 예산 → `FRAME_BUDGET.md` / 스택 선택 근거 → `PIPELINE_STACK.md` /
> 역할 경계 → `KICKOFF_ROLES.md` / 미결질문 → `docs/research/RESEARCH_20260728_PIPELINE.md`

**읽는 법:** 각 칸은 `제안값 (팀원2)` + `확정: ☐ 미정` 형태다.
**동의하면 체크만, 다르면 값을 덮어쓰고 이유 한 줄.** 침묵은 동의로 간주하고 제안값으로 구현을 시작한다.

| 계약 | 주는 사람 → 받는 사람 | 이 계약이 채우는 [프레임 버짓](FRAME_BUDGET.md) 칸 | 마감 |
|---|---|---|---|
| **A. 모델 아티팩트 규약** | 팀원1 → 팀원2 | E, F, G (탐지 전처리·추론·후처리) | 이번 주 |
| **B. ①②단계 모듈 시그니처** | 팀장 → 팀원2 | C, D (눈부심 억제·저조도 개선) | 이번 주 |
| **C. 녹화 데모 입력 규약** | 팀원3 → 팀원2 | (데모 모드 입력 경로) | **대량 촬영 전** |

---

## 공통 원칙 (세 계약 모두에 적용)

1. **런타임은 아무것도 추측하지 않는다.** 하드코딩된 가정(해상도·정규화 상수·클래스 순서)은 전부 버그의 씨앗이다.
   모든 값은 계약서 또는 아티팩트에 동봉된 메타데이터에서 읽는다.
2. **골든 샘플 없는 인수는 없다.** 각 계약은 `입력 예시 + 기대 출력` 한 쌍을 반드시 포함한다.
   이게 있어야 "내 쪽은 맞는데?"를 30초 만에 판정한다. (§A-6, §B-6, §C-6)
3. **버전을 붙인다.** 규약이 바뀌면 버전을 올리고, 런타임은 자기가 아는 버전만 로드한다.
   조용히 바뀐 규약이 데모 전날 터지는 걸 막는다.
4. **예산이 계약의 일부다.** 각 모듈은 [프레임 버짓](FRAME_BUDGET.md)에 자기 칸을 갖는다.
   **단계별 배분은 아직 빈칸이고 실측으로 채운다**(버짓표 v0.2 §3) — 배정된 ms가 있어서가 아니라
   **자기 비용을 재서 보고할 의무**가 계약의 일부라는 뜻이다.
   프레임 예산(66.7ms)을 압박하면 숨기지 말고 통보 — 레버는 팀 합의로 당긴다(버짓표 §5).

---

# 계약 A — 모델 아티팩트 규약 (팀원1 → 팀원2)

**목적:** 팀원1이 학습·export한 검출 모델을 팀원2가 **코드 수정 없이** 런타임에 꽂을 수 있게 한다.
**대상 버짓 칸:** E(전처리) / F(추론) / G(후처리) — 세 칸 모두 **미측정**(버짓표 v0.2 §3)

## A-1. 포맷·런타임 호환

| 항목 | 제안값 (팀원2) | 확정 | 왜 이 값인가 / 다르면 무슨 일이 |
|---|---|---|---|
| 포맷 | ONNX | ☐ | ORT Mobile 전제 |
| opset | **13** | ☐ | 너무 높으면 모바일 EP(NNAPI/GPU delegate) 미지원 연산자 발생 → CPU fallback |
| batch | **1 고정 (static)** | ☐ | 동적 배치는 EP 파티셔닝을 깨뜨림 |
| **입력 shape** | **완전 static (동적 축 금지)** | ☐ | ⚠️ **가장 중요.** 동적 shape이면 GPU/NNAPI가 서브그래프를 못 잡고 통째로 CPU로 떨어진다 → **F 칸이 통째로 무너지는 경로** |
| 정밀도 | 1차 **fp32**, 2차 fp16 | ☐ | fp32로 베이스라인 확보 후 fp16 비교. int8은 저조도 정확도 검증 통과 후에만 |

## A-2. 입력 텐서

| 항목 | 제안값 | 확정 | 비고 |
|---|---|---|---|
| 입력 해상도 | **640×640** (대안 512, 416) | ☐ | **버짓표 §7 질문 1.** F 실측의 전제이자, 예산 초과 시 1순위 레버(버짓표 §5) |
| 레이아웃 | NCHW `[1,3,H,W]` | ☐ | |
| 채널 순서 | **RGB** | ☐ | BGR이면 색이 뒤집힌 채로 "그럭저럭" 동작해 발견이 늦다 |
| dtype / 범위 | float32, **0.0~1.0 (`/255`)** | ☐ | mean/std 정규화를 쓴다면 정확한 상수 명시 |
| mean / std | **없음** (단순 `/255`) | ☐ | 있으면 값 기입: mean=[ ], std=[ ] |
| 리사이즈 방식 | **letterbox (종횡비 유지)** | ☐ | 720p(16:9) → 정사각 입력이므로 필수 |
| letterbox pad 색 | **(114,114,114)** | ☐ | 학습 시 값과 반드시 동일해야 함 |
| letterbox pad 위치 | **상하 균등 분배(center)** | ☐ | ⚠️ 이게 다르면 **박스가 통째로 세로로 밀린다.** 학습 파이프라인과 일치시킬 것 |

> **왜 이렇게 잘게 묻나:** E(전처리)는 이 표를 그대로 구현하는 비용이다.
> 하나라도 틀리면 모델은 "돌긴 도는데 박스가 이상한" 상태가 되고, 이건 디버깅이 가장 오래 걸리는 실패 모드다.

## A-3. 출력 텐서

팀장 선행 프로젝트(AID)가 YOLO11s 계열이므로 **v8/v11 헤드**를 기본 가정한다.

| 항목 | 제안값 | 확정 | 비고 |
|---|---|---|---|
| 출력 개수 | **1개** | ☐ | |
| shape | **`[1, 4+nc, N]`** (v8/v11 헤드, transpose 필요) | ☐ | v5 계열이면 `[1, N, 5+nc]`(objectness 포함) — **어느 쪽인지 명시 필수** |
| objectness 채널 | **없음** (v8/v11) | ☐ | 있으면 후처리 수식이 달라짐 |
| N (앵커 수) | 640→**8400**, 512→5376, 416→3549 | ☐ | 입력 해상도에 종속 |
| 박스 인코딩 | **cx, cy, w, h** | ☐ | x1y1x2y2면 명시 |
| 좌표 단위 | **letterbox 입력 픽셀 기준** (0~640) | ☐ | 0~1 정규화면 명시. ⚠️ 이걸 틀리면 박스가 화면 구석에 뭉친다 |
| 클래스 점수 | sigmoid **적용 완료** (0~1) | ☐ | raw logit이면 런타임에서 sigmoid 필요 |
| **NMS 내장** | **❌ 내장하지 않음 (raw 출력)** | ☐ | **↓ 아래 근거 참조** |

### NMS를 모델에 넣지 말자는 이유 (팀원2 의견)

1. **성능:** ONNX `NonMaxSuppression` 연산자는 모바일 GPU/NNAPI EP에서 대부분 미지원 → 그래프가 그 지점에서
   쪼개져 CPU로 떨어지고, **텐서가 GPU↔CPU를 왕복**한다. F 칸이 이것 하나로 무너질 수 있다.
2. **튜닝:** conf/IoU 임계값을 **실기기에서** 조정해야 한다. 야간 저조도는 임계값 민감도가 크고,
   재export 없이 슬라이더로 돌려보는 게 압도적으로 빠르다.
3. **책임 분리:** 버짓표에서 **G(후처리)는 팀원2 칸**이다. 런타임이 쥐고 있어야 예산 관리가 된다.

→ **팀원1 요청사항: NMS·decode 없는 raw 출력으로 export.** 대신 팀원2가 후처리를 구현하고,
학습 파이프라인과 동일한 결과가 나오는지 골든 샘플(§A-6)로 검증한다.

## A-4. 클래스 인덱스 테이블

| index | 클래스명 | 우선순위 | 비고 |
|---|---|---|---|
| 0 | `stairs` (계단) | 최상 | 신청서 명시 대상 |
| 1 | `person` (보행자) | 상 | 신청서 명시 대상 |
| 2 | ☐ (미정) | | 예: `pole`, `curb`, `vehicle`, `obstacle` |

**불변 규칙 (팀원2 요청):**
- **인덱스는 재배치 금지.** 클래스 추가는 **항상 끝에 append.** 중간 삽입은 이전에 측정한 모든 성능·안전 수치를 무효화한다.
- 클래스가 늘면 출력 shape의 `4+nc`가 바뀌므로 **런타임에 통보 필수.**
- ④단계 강조 스타일(색·굵기·깜빡임)이 클래스별로 달라지므로, 클래스 추가 = 렌더 규약 변경 = 팀원2 작업 발생.

## A-5. 파일 전달 규약

```
models/
  bammasil_det_v3_640_fp32.onnx
  bammasil_det_v3_640_fp32.json     ← 동봉 메타데이터 (필수)
  golden/
    sample_night_stairs.png          ← 입력 원본 (720p)
    sample_night_stairs.expected.json ← 기대 출력 박스
```

**명명 규칙:** `bammasil_det_v<버전>_<입력해상도>_<정밀도>.onnx`

**동봉 메타데이터 `.json` (런타임이 이걸 읽고 자기를 설정한다 — 하드코딩 제거):**
```json
{
  "spec_version": "1.0",
  "model_version": 3,
  "input": {
    "name": "images",
    "shape": [1, 3, 640, 640],
    "layout": "NCHW",
    "channel_order": "RGB",
    "scale": 0.00392156862,
    "mean": [0.0, 0.0, 0.0],
    "std": [1.0, 1.0, 1.0],
    "letterbox": { "enabled": true, "pad_value": [114, 114, 114], "align": "center" }
  },
  "output": {
    "name": "output0",
    "shape": [1, 6, 8400],
    "head": "v8",
    "box_encoding": "cxcywh",
    "coord_space": "letterbox_pixels",
    "class_scores_activated": true,
    "nms_included": false
  },
  "classes": ["stairs", "person"],
  "recommended_thresholds": { "conf": 0.35, "iou": 0.45 },
  "trained_on": "야간 자체촬영 v2 + 공개데이터",
  "sha256": "..."
}
```

## A-6. 인수 조건 (Definition of Done)

팀원2가 "받았다"고 인정하는 조건:

- [ ] `.onnx` + `.json` 메타 동봉
- [ ] **골든 샘플 1쌍 이상** — 야간 720p 입력 이미지 + 그 이미지에 대한 기대 박스(클래스·좌표·점수)
      → 런타임 통합 결과가 이것과 **±2px 이내**로 일치하면 통합 성공으로 판정
- [ ] PC(파이썬) 기준 추론 시간 참고치 1줄 (모바일 예측의 출발점)
- [ ] 규약 변경 시 `model_version` 증가 + 변경점 한 줄

> **골든 샘플을 고집하는 이유:** 이게 없으면 박스가 이상할 때 "모델 문제냐 런타임 문제냐"로 서로를 의심하며
> 며칠을 태운다. 있으면 30초에 판정 나고, 이후 **모델이 갱신될 때마다 자동 회귀 테스트**로 재사용된다.

---

# 계약 B — ①②단계 모듈 시그니처 (팀장 → 팀원2)

**목적:** 팀장이 설계한 눈부심 억제·저조도 개선 알고리즘을 팀원2가 **모바일 GPU 위에 옮겨** 프레임 버짓 안에 넣는다.
**경계 재확인:** *알고리즘 설계·파라미터 = 팀장 / 폰 위 실현·예산 = 팀원2* (`KICKOFF_ROLES.md` 결정 2)
**대상 버짓 칸:** C(①) / D(②) — 두 칸 모두 **미측정**(버짓표 v0.2 §3)

## B-1. 실행 위치 — 가장 중요한 한 칸

| 항목 | 제안값 (팀원2) | 확정 | 영향 |
|---|---|---|---|
| 실행 위치 | **GPU (OpenGL ES / Vulkan 셰이더)** | ☐ | ⚠️ **CPU 구현이면 C·D가 프레임 예산 안에 들어오지 못한다.** 720p 픽셀 단위 처리를 CPU로 하면 수십 ms |
| 입력 형태 | **GPU 텍스처** | ☐ | CPU 버퍼면 매 프레임 GPU↔CPU 왕복 비용이 추가로 붙는다 |
| 출력 형태 | **GPU 텍스처** (FBO 렌더 타겟) | ☐ | ③탐지 입력용으로 다운스케일 readback은 팀원2가 별도 처리 |
| in-place 여부 | ❌ 별도 출력 텍스처 (핑퐁) | ☐ | 셰이더는 읽는 텍스처에 동시에 쓸 수 없음 |

> **팀장께 드리는 질문:** PC 프로토타입이 OpenCV/numpy CPU 기반이어도 괜찮습니다 —
> **그게 레퍼런스(§B-6)가 되고, 셰이더 포팅은 제가 합니다.** 다만 알고리즘이 **픽셀 병렬화 가능한 형태**여야 합니다.
> 전역 정렬·반복 최적화·큰 커널 순회처럼 병렬화가 어려운 연산이 들어가면 미리 알려주세요 — 설계 단계에서 대안을 찾는 게 쌉니다.

## B-2. 색공간 — 예산 절감 논점

| 항목 | 제안값 | 확정 | 비고 |
|---|---|---|---|
| 카메라 원본 | YUV420 (Android `YUV_420_888`) | ☐ | 폰 카메라의 기본 출력 |
| **①②의 동작 평면** | **Y(휘도) 평면만 처리, UV는 통과** | ☐ | **↓ 근거 참조** |
| 대안 | RGB 텍스처로 변환 후 처리 | ☐ | 색상 기반 처리가 필요하면 이쪽 |
| 감마 | sRGB(비선형) 그대로 vs 선형화 후 처리 | ☐ | 대비 복원은 선형 공간이 물리적으로 옳지만 변환 비용 발생 |

**Y 평면만 처리하자는 제안 근거:** ①눈부심 억제·②저조도 개선은 본질적으로 **휘도 조작**이다.
Y만 처리하면 (a) 처리 픽셀 수가 1/1.5로 줄고 (b) YUV→RGB 전체 변환(버짓 B 칸)을 늦추거나 줄일 수 있으며
(c) **색상이 원본 그대로 보존**된다 — 저시력 사용자에게 색 왜곡은 그 자체로 위험 신호를 흐릴 수 있다.
→ **팀장 판단 필요:** 알고리즘이 색상 정보를 필요로 하는가?

## B-3. 모듈 시그니처 (개념 형태)

```
stage1_glare_suppress(tex_in, tex_out, params1)   // ① 눈부심 억제
stage2_lowlight_enhance(tex_in, tex_out, params2) // ② 저조도 개선
```

| 항목 | 제안값 | 확정 | 비고 |
|---|---|---|---|
| 실행 순서 | **① → ②** (고정) | ☐ | 순서 의존성이 있으면 명시. 뒤집으면 결과가 달라지는가? |
| 해상도 | 입출력 동일 (720p) | ☐ | 내부적으로 다운샘플 사용 시 명시 |
| 패스 수 | ①=1패스, ②=☐ | ☐ | 멀티패스(예: 블러 후 합성)면 중간 텍스처 개수 명시 → 메모리·대역폭 영향 |

## B-4. 프레임 간 상태 (temporal) — 미리 물어야 하는 것

**질문: ①②가 이전 프레임 정보를 쓰나요?** (예: 노출 적응, 히스토그램 시간 평활, 이전 프레임 블렌딩)

| 상태 사용 | 런타임이 추가로 해야 하는 일 |
|---|---|
| **아니오 (stateless)** | 없음. 가장 단순하고 안전 |
| **예 (stateful)** | 상태 버퍼 할당·수명 관리 + **리셋 조건 정의 필요** |

stateful이면 아래를 팀장이 정의해 주셔야 합니다:
- 리셋 트리거: 앱 재개 / 카메라 전환 / 장면 급변 / 프레임 드롭 발생 시?
- 수렴 시간: 몇 프레임 만에 안정되나? (데모 시작 직후 화면이 출렁이면 시연 사고)
- ⚠️ **temporal 상태는 프레임 드롭과 상호작용한다.** 프레임을 건너뛴 뒤 상태가 튀면 화면이 번쩍인다 —
  야간 보행 보조에서 화면 번쩍임은 단순 결함이 아니라 **안전 문제**다.

## B-5. 튜닝 파라미터 노출

**요청:** 알고리즘 상수를 셰이더 내부에 하드코딩하지 말고 **uniform으로 노출**해 주세요.
그러면 팀원2가 실기기에 디버그 슬라이더를 붙여, **팀장이 실제 야간 현장에서 직접 값을 돌려보고 확정**할 수 있습니다.
PC 모니터에서 정한 값과 야간 실기기 화면에서 좋은 값은 거의 항상 다릅니다.

| 파라미터 | 의미 | 범위 | 기본값 |
|---|---|---|---|
| ☐ | | | |
| ☐ | | | |

## B-6. 인수 조건 (Definition of Done)

- [ ] **PC 레퍼런스 구현** (파이썬/OpenCV 등, 언어 무관 — 정확한 동작 정의가 목적)
- [ ] **골든 이미지 쌍** — 야간 입력 프레임 + 레퍼런스를 통과한 기대 출력 이미지
      → 팀원2의 셰이더 포팅 결과가 이것과 **픽셀 차 허용오차 이내**면 포팅 성공으로 판정
- [ ] 파라미터 표(§B-5) 기입
- [ ] temporal 상태 여부(§B-4) 답변

> **이 두 가지가 "알고리즘=팀장 / 실현=팀원2" 경계를 실행 가능하게 만드는 장치입니다.**
> 레퍼런스와 골든 이미지가 있으면, 포팅 결과가 다를 때 그건 명백히 **제 책임**이고 제가 고칩니다.
> 없으면 "제 알고리즘이 원래 이렇게 나오나요?"를 서로 묻는 상태가 되어 경계가 무너집니다.

---

# 계약 C — 녹화 데모 입력 규약 (팀원3 → 팀원2)

**목적:** 라이브 카메라 대신 녹화 영상을 파이프라인에 흘리는 **데모 모드**의 입력 규격.
**용도 3가지:** ① 발표장 조명에서 야간 시연이 불가능할 때의 **백업 경로**(라이브 시연 실패 시 보험)
② 성능·안전 **회귀 테스트의 고정 입력**(같은 영상 = 재현 가능한 비교) ③ 개발 중 야외 촬영 없이 반복 테스트

**⚠️ 이 계약만 마감이 다르다: 대량 촬영 전에 확정해야 한다.** 규약이 틀린 채로 100개를 찍으면 재촬영이 불가능하다.
→ **파일럿 3클립 먼저 → 팀원2 검증 → 그다음 대량 촬영**을 강력히 권합니다.

## C-1. 컨테이너·코덱

| 항목 | 제안값 | 확정 | 비고 |
|---|---|---|---|
| 컨테이너 | **MP4** | ☐ | |
| 코덱 | **H.264 (AVC)** | ☐ | HEVC/H.265는 기기별 하드웨어 디코더 호환 편차가 큼. 데모 안정성 우선 |
| 프로파일 | High, Level 4.0 이하 | ☐ | |
| 비트 심도 | 8bit 4:2:0 | ☐ | |
| **프레임레이트** | **30fps 고정 (CFR)** | ☐ | ⚠️ **폰 카메라 기본은 VFR(가변)이다.** VFR이면 프레임 간격이 들쭉날쭉해 **성능 측정 자체가 오염된다** |
| 해상도 | **1280×720** | ☐ | 파이프라인 목표 해상도와 일치 |
| **비트레이트** | **20 Mbps 이상** | ☐ | ↓ 아래 근거 |
| 회전 메타데이터 | **0° (가로 촬영, 회전 메타 없음)** | ☐ | ⚠️ 세로 촬영은 "가로 영상 + 90° 회전 메타"로 저장되어 디코더 출력이 눕는다 |
| 오디오 | 불필요 (있어도 무시) | ☐ | |

**비트레이트를 높게 잡는 이유 (어두운 영상 특유의 함정):**
어두운 장면은 계조가 좁은 구간에 몰려 있어 **압축 밴딩(계단 모양 얼룩)**이 심하게 생긴다.
그런데 우리 ②단계는 바로 그 어두운 계조를 **끌어올려 늘리는** 처리다.
→ **압축 밴딩이 함께 증폭되어, 알고리즘 성능이 실제보다 나쁘게 보인다.**
녹화 영상으로 측정한 ②단계 품질이 라이브 카메라보다 나쁘다면 십중팔구 이 문제다.
가능하면 **무압축/저압축 원본을 따로 보관**해 주시면 알고리즘 검증용으로 씁니다.

## C-2. 촬영 설정 — 가장 중요한 항목

> **⚠️ 폰의 야간모드/HDR/자동보정을 반드시 끄고 촬영해 주세요.**
>
> 최신 폰의 나이트모드는 **이미 저조도 개선을 해버린다.** 그 영상으로 데모하면:
> - 우리 ②단계가 개선할 여지가 이미 사라져 **효과가 없어 보이고**
> - "폰 기본 카메라로 충분한데 왜 이 앱이 필요한가"라는 **심사 질문을 자초한다**
> - 무엇보다 **실제 사용 상황(맨눈으로 보는 야간)과 다른 영상**이 된다
>
> 우리가 보여줘야 할 것은 **"사람 눈에 보이는 그대로의 어두움"**이고, 밤마실이 그걸 개선하는 모습입니다.

| 항목 | 제안값 | 확정 |
|---|---|---|
| 야간모드 / Night Sight | **OFF** | ☐ |
| HDR | **OFF** | ☐ |
| 자동 노출(AE) | 가능하면 **수동 고정** | ☐ |
| 자동 화이트밸런스 | 가능하면 고정 | ☐ |
| 손떨림 보정(OIS/EIS) | ON 허용 (실사용과 동일) | ☐ |
| 카메라 높이·각도 | 실제 사용 자세(가슴~눈높이, 전방 하향) | ☐ |

## C-3. 필수 클립 세트 (회귀 테스트 케이스 겸용)

각 클립이 특정 단계의 테스트 케이스가 되도록 **의도적으로** 찍어 주시면, 그대로 회귀 테스트 자산이 됩니다.

| # | 장면 | 검증 대상 | 길이 |
|---|---|---|---|
| 1 | **마주 오는 차 헤드라이트 / 강한 가로등 직시** | ① 눈부심 억제 | 15~30초 |
| 2 | **가로등 없는 어두운 보도 직진** | ② 저조도 개선 | 15~30초 |
| 3 | **계단 하강 접근** (밤마실 대표 시나리오) | ③ 탐지 + ④ 강조 | 15~30초 |
| 4 | **저조도에서 보행자 마주침** | ③ 탐지 | 15~30초 |
| 5 | **밝은 곳 → 어두운 곳 급전환** (골목 진입 등) | ①② 시간적 안정성 (§B-4) | 15~30초 |
| 6 | **무광 장애물** (볼라드, 검은 기둥, 턱) | ③ 난이도 상한 확인 | 15~30초 |
| 7 | *(선택)* **동일 코스 주간 촬영본** | 라벨링 기준 / 발표 비교용 | 코스 전체 |

> **7번이 특히 유용합니다:** 같은 코스의 주간 영상이 있으면 "야간에 뭐가 있었어야 하는지"의 정답지가 되어
> 팀원1의 라벨링과 팀원2의 안전 회귀 검증(위험물 강조 누락률) 양쪽에 쓰입니다.
> 그리고 발표 슬라이드의 **주간/야간/밤마실 3단 비교**가 그대로 나옵니다.

## C-4. 파일 명명·메타

```
clips/
  nw_001_glare_headlight_720p30.mp4
  nw_001_glare_headlight.json
```
`nw_<번호>_<시나리오>_<해상도><fps>.mp4`

**동봉 메타 `.json`:**
```json
{
  "clip_id": "nw_001",
  "scenario": "glare_headlight",
  "location": "○○동 △△로 보도",
  "datetime": "2026-07-28T21:40:00+09:00",
  "device": "기종명",
  "camera_settings": { "night_mode": false, "hdr": false, "ae_locked": true },
  "resolution": "1280x720", "fps": 30, "duration_sec": 22,
  "notes": "우측에서 차량 2대 교행, 21초 지점 최대 글레어"
}
```
> `notes`의 **시각 지점 표시**가 특히 유용합니다 — "21초 지점"처럼 적어 주시면
> 회귀 테스트에서 그 구간만 잘라 반복 검증할 수 있습니다.

## C-5. 프라이버시 (신청서 약속과 직결)

신청서가 "서버 전송·저장 없음"을 프라이버시 차별점으로 내세웠으므로, **촬영 자산도 같은 기준으로 다룹니다.**

- 촬영 중 **행인 얼굴·차량 번호판**이 식별 가능하게 담기면, 발표·공개 자료에 쓸 때 문제가 됩니다.
- 발표/제출용 클립은 **얼굴·번호판 블러 처리본**을 따로 준비 (원본은 팀 내부 검증용으로만).
- 사유지·CCTV 금지구역 촬영 회피.
- ⚠️ 클립을 공개 저장소·공유 링크에 올리기 전에 팀에 먼저 확인.

## C-6. 인수 조건 (Definition of Done)

- [ ] **파일럿 3클립 (시나리오 1·2·3) 먼저 전달** → 팀원2가 디코딩·프레임간격·밴딩 검증 후 GO 통보
- [ ] GO 이후 나머지 클립 촬영
- [ ] 각 클립에 메타 `.json` 동봉
- [ ] 야간모드/HDR OFF 확인 (§C-2)

---

## 부록 — 이 문서가 하는 일 (한 문단)

이 세 계약은 **팀원2가 남에게 무엇을 요구하는 문서가 아니라, 팀원2가 무엇을 책임지는지 선언하는 문서**다.
계약이 지켜지면 통합 실패는 전적으로 팀원2 책임이고, 그 대가로 **인터페이스 정의권**을 갖는다.
빈 칸이 채워지는 순간 [프레임 버짓](FRAME_BUDGET.md)의 C·D·E·F·G가 실측 가능한 상태가 되고,
그때부터 "720p 15FPS"는 구호가 아니라 **추적 가능한 숫자**가 된다.

**변경 이력**
- v0.1 (2026-07-24, 팀원2) — 최초 작성. 세 계약 모두 상대방 확정 대기.
- **v0.2 (2026-07-29, 팀원2)** — **계약값 변경 없음.** `☐` 항목과 제안값은 v0.1 그대로다.
  `FRAME_BUDGET.md` v0.2가 단계별 잠정 배정치를 제거함에 따라, 본문이 인용하던 옛 배정치
  (E 5ms / F 22ms / G 3ms / C 6ms / D 10ms / B 4ms)를 **칸 이름 참조로 교체**했다.
  → 계약을 검토 중이셨다면 **다시 읽으실 필요 없습니다.** 확정해 주실 값은 그대로입니다.
